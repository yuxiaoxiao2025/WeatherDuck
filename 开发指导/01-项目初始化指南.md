# 天气鸭项目开发指导 - 阶段1：项目初始化

## 🛠 修订记录
- 文档版本：v1.2
- 更新日期：2025-11-13
- 维护团队：天气鸭开发团队
- 变更说明：与阶段2最新规范保持一致，统一采用 Tailwind CSS 与设计令牌体系；安装命令统一为 `cnpm`（如无 `cnpm` 可用 `npm`）；同步测试栈与目录规范。

## 📋 阶段概述

本阶段将完成项目的基础环境搭建、技术栈初始化和开发规范配置，为后续开发工作奠定坚实基础。

---

## 🎯 核心目标

- ✅ 建立完整的开发和预览环境
- ✅ 配置项目依赖和构建工具
- ✅ 实施代码规范和质量检查工具
- ✅ 验证跨平台架构可行性

---

## 📖 适用规范清单

本阶段开发遵循以下规范要求,所有规范文件位于 `.qoder/rules/` 目录:

### 核心规范

#### 1. 开发需求规范
文件路径: `.qoder/rules/requirements-spec.zh-CN.md`

- **[规则 1] 生成完整可运行代码** → 第20行
  - 代码必须可立即执行,不允许使用占位符
  - 包含所有必要的 import、依赖和配置

- **[规则 3] 最小化新增依赖** → 第69行
  - 优先使用项目现有依赖
  - 添加新依赖需要充分理由

- **[规则 9] 功能优先于完美** → 第163行
  - 可工作的代码 > 完美的代码
  - 先让项目运行起来，再优化

- **[规则 10] 确保代码成功编译** → 第171行
  - 所有代码必须能编译/运行且无错误
  - 验证 import 和依赖是否正确

- **[规则 13] 只使用真实存在的库** → 第199行
  - 导入前验证包是否存在
  - 确认库版本兼容性

#### 2. 工作流规范
文件路径: `.qoder/rules/workflow-spec.zh-CN.md`

- **[规则 2] 版本号管理** → 第50行
  - 遵循语义化版本控制(MAJOR.MINOR.PATCH)
  - 发布时更新 package.json 中的版本

- **[规则 6] 文档同步** → 第141行
  - 修改代码时更新相关文档
  - 添加新功能时更新 README

- **[规则 10] 依赖更新策略** → 第229行
  - 定期审查依赖更新
  - 避免已弃用或无人维护的包

#### 3. 命名约定
文件路径: `.qoder/rules/naming-conventions.zh-CN.md`

- **[约定 5] 文件命名** → 第154行
  - 文件使用 kebab-case(小写带连字符)
  - 在目录中分组相关文件

- **[约定 9] 环境变量命名** → 第277行
  - 使用 UPPER_SNAKE_CASE
  - 使用公共前缀分组相关变量

### 质量规范

#### 4. 安全规范
文件路径: `.qoder/rules/security-spec.zh-CN.md`

- **[规则 4] 依赖安全管理** → 第128行
  - 定期运行安全扫描(npm audit)
  - 不使用已知漏洞的依赖版本

- **[规则 8] 安全配置管理** → 第231行
  - 不在代码中硬编码密钥、密码、API 令牌
  - 使用环境变量管理配置
  - 不提交 .env 文件到版本控制

### 项目设计参考文档

#### 技术方案文档
文件路径: `技术方案v1.0.md`

- **架构选型** → 第7-83行
  - 双目标混合架构(桌面版+Web版)
  - 平台支持和统一代码库
  - 渐进式体验设计

- **技术栈选型** → 第124-285行
  - Electron + React + Vite
  - Context7工具验证结果
  - 构建和部署工具链

---

## 🚀 实施步骤

### 步骤 1.1：创建项目目录结构

**适用规范**: 
- `.qoder/rules/naming-conventions.zh-CN.md` - [约定 5] 文件命名 (第153行)
  - 文件使用 kebab-case（小写带连字符）

**操作清单**:
```powershell
# 在 Windows Desktop Clock Utility 目录下执行
mkdir src
mkdir src\components
mkdir src\hooks
mkdir src\utils
mkdir src\types
mkdir src\adapters
mkdir src-electron
mkdir src-web
mkdir public
mkdir tests
mkdir config
mkdir docs
```

**目录说明**:
```
Windows Desktop Clock Utility/
├── src/                    # 共享源代码
│   ├── components/         # React UI组件
│   ├── hooks/             # 自定义Hooks
│   ├── utils/             # 工具函数
│   ├── types/             # TypeScript类型定义
│   └── adapters/          # 平台适配器
├── src-electron/          # Electron主进程代码
├── src-web/              # Web版特定代码
├── public/               # 静态资源
├── tests/                # 测试文件
├── config/               # 配置文件
├── docs/                 # 项目文档
└── .qoder/rules/         # 项目规范（已存在）
```

**验收标准**:
- [ ] 所有目录使用kebab-case命名
- [ ] 目录结构清晰，职责明确
- [ ] 符合技术方案中的架构设计

---

### 步骤 1.2：初始化 Electron + Vite + React 项目

**适用规范**: 
- `.qoder/rules/requirements-spec.zh-CN.md` - [规则 13] 只使用真实存在的库 (第199行)
  - 导入前验证包是否存在，确认库版本兼容性
- `.qoder/rules/requirements-spec.zh-CN.md` - [规则 3] 最小化新增依赖 (第69行)
  - 优先使用项目现有依赖，添加新依赖需要充分理由
- `.qoder/rules/requirements-spec.zh-CN.md` - [规则 9] 功能优先于完美 (第163行)
  - 先让项目初始化成功，后续再优化配置

**操作清单**:

```powershell
# 1. 初始化 npm 项目
npm init -y

# 2. 安装核心依赖（基于技术方案验证的真实库）
cnpm install react@18 react-dom@18 ; cnpm install electron@28

# 3. 安装开发依赖
cnpm install -D vite@5 ; cnpm install -D @vitejs/plugin-react
cnpm install -D typescript@5 ; cnpm install -D @types/react @types/react-dom ; cnpm install -D @types/node

# 4. 安装 Electron 构建工具
cnpm install -D electron-builder ; cnpm install -D electron-vite
```

**package.json 配置** (遵循 `.qoder/rules/workflow-spec.zh-CN.md` - [规则 2] 版本号管理，第49行):

```json
{
  "name": "weather-duck",
  "version": "0.1.0",
  "description": "天气鸭 - 现代化桌面天气应用",
  "main": "dist-electron/main.js",
  "type": "module",
  "scripts": {
    "dev:electron": "electron-vite dev",
    "dev:web": "vite",
    "build:electron": "electron-vite build",
    "build:web": "vite build",
    "preview:web": "vite preview",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts,.tsx",
    "test": "vitest"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.0",
    "electron": "^28.0.0",
    "electron-builder": "^24.9.0",
    "electron-vite": "^2.0.0",
    "typescript": "^5.3.0",
    "vite": "^5.0.0"
  }
}
```

**验收标准**:
- [ ] 所有依赖都是真实存在的库（符合Context7验证结果）
- [ ] 版本号遵循语义化版本控制（MAJOR.MINOR.PATCH）
- [ ] package.json 包含完整的项目信息
- [ ] 依赖安装成功，无错误
- [ ] 最小化依赖（规则3：仅安装必需库）
- [ ] 功能优先（规则9：项目可初始化成功）

---

### 步骤 1.3：配置 TypeScript

**适用规范**: 
- `.qoder/rules/requirements-spec.zh-CN.md` - [规则 10] 确保代码成功编译 (第170行)
  - 所有代码必须能编译/运行且无错误，验证 import 和依赖是否正确

**创建 tsconfig.json**:

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,

    /* Bundler mode */
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx",

    /* Linting */
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,

    /* Path Alias */
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"],
      "@components/*": ["src/components/*"],
      "@hooks/*": ["src/hooks/*"],
      "@utils/*": ["src/utils/*"],
      "@types/*": ["src/types/*"]
    }
  },
  "include": ["src"],
  "references": [{ "path": "./tsconfig.node.json" }]
}
```

**验收标准**:
- [ ] TypeScript 配置正确
- [ ] 路径别名配置生效
- [ ] 编译检查无错误

---

### 步骤 1.4：配置 Vite 构建工具

**适用规范**: 
- `.qoder/rules/requirements-spec.zh-CN.md` - [规则 1] 生成完整可运行代码 (第19行)
  - 代码必须可立即执行，包含所有必要的配置

**创建 vite.config.ts**:

```typescript
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import path from 'path';

export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
      '@components': path.resolve(__dirname, './src/components'),
      '@hooks': path.resolve(__dirname, './src/hooks'),
      '@utils': path.resolve(__dirname, './src/utils'),
      '@types': path.resolve(__dirname, './src/types')
    }
  },
  server: {
    port: 3000,
    hmr: true
  },
  build: {
    outDir: 'dist-web',
    rollupOptions: {
      input: {
        main: path.resolve(__dirname, 'index.html')
      }
    }
  },
  define: {
    __APP_VERSION__: JSON.stringify(process.env.npm_package_version),
    __BUILD_TIME__: JSON.stringify(new Date().toISOString())
  }
});
```

**验收标准**:
- [ ] Vite 配置正确
- [ ] HMR 热重载功能正常
- [ ] 路径别名解析正确

---

### 步骤 1.5：配置环境变量管理

**适用规范**: 
- `.qoder/rules/security-spec.zh-CN.md` - [规则 8] 安全配置管理 (第230行)
  - 不在代码中硬编码密钥，使用环境变量，不提交 .env 文件到版本控制
- `.qoder/rules/naming-conventions.zh-CN.md` - [约定 9] 环境变量命名 (第276行)
  - 使用 UPPER_SNAKE_CASE，使用公共前缀分组相关变量

**创建 .env.example**:

```env
# 和风天气API配置
VITE_WEATHER_API_KEY=your_api_key_here
VITE_WEATHER_API_HOST=https://devapi.qweather.com

# 和风天气GeoAPI配置
VITE_GEO_API_HOST=https://geoapi.qweather.com

# 应用配置
VITE_APP_NAME=天气鸭
VITE_APP_VERSION=0.1.0
VITE_APP_ENV=development

# 调试配置
VITE_DEBUG_MODE=true
VITE_ENABLE_DEVTOOLS=true
```

**创建 .gitignore**:

```gitignore
# 依赖
node_modules/
dist/
dist-electron/
dist-web/

# 环境变量
.env
.env.local
.env.*.local

# 构建输出
out/
release/

# 系统文件
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# 日志
*.log
logs/

# 测试覆盖率
coverage/
.nyc_output/

# 临时文件
*.tmp
*.bak

# SQLite数据库
*.sqlite
*.sqlite3
*.db
```

**验收标准**:
- [ ] 环境变量使用 UPPER_SNAKE_CASE 命名
- [ ] .env 文件已加入 .gitignore
- [ ] .env.example 作为模板提供
- [ ] 敏感信息不会被提交到版本控制

---

### 步骤 1.6：配置代码规范工具

**适用规范**: 
- `.qoder/rules/workflow-spec.zh-CN.md` - [规则 6] 文档同步 (第140行)
  - 保持代码与配置文件的一致性

**安装 ESLint 和 Prettier**:

```powershell
npm install -D eslint prettier
npm install -D @typescript-eslint/parser @typescript-eslint/eslint-plugin
npm install -D eslint-plugin-react eslint-plugin-react-hooks
npm install -D eslint-config-prettier eslint-plugin-prettier
```

**创建 .eslintrc.json**:

```json
{
  "env": {
    "browser": true,
    "es2021": true,
    "node": true
  },
  "extends": [
    "eslint:recommended",
    "plugin:react/recommended",
    "plugin:react-hooks/recommended",
    "plugin:@typescript-eslint/recommended",
    "prettier"
  ],
  "parser": "@typescript-eslint/parser",
  "parserOptions": {
    "ecmaFeatures": {
      "jsx": true
    },
    "ecmaVersion": "latest",
    "sourceType": "module"
  },
  "plugins": ["react", "@typescript-eslint"],
  "rules": {
    "react/react-in-jsx-scope": "off",
    "@typescript-eslint/no-unused-vars": ["error", { "argsIgnorePattern": "^_" }],
    "no-console": ["warn", { "allow": ["warn", "error"] }]
  },
  "settings": {
    "react": {
      "version": "detect"
    }
  }
}
```

**创建 .prettierrc.json**:

```json
{
  "semi": true,
  "trailingComma": "es5",
  "singleQuote": true,
  "printWidth": 100,
  "tabWidth": 2,
  "useTabs": false,
  "arrowParens": "always",
  "endOfLine": "auto"
}
```

**验收标准**:
- [ ] ESLint 配置正确
- [ ] Prettier 配置正确
- [ ] 代码格式化正常工作
- [ ] 与规范文件保持一致

---

### 步骤 1.7：创建基础项目文件

**适用规范**: 
- `.qoder/rules/requirements-spec.zh-CN.md` - [规则 1] 生成完整可运行代码 (第19行)
  - 提供完整实现，包含所有必要的 import 和配置

**创建 index.html**:

```html
<!DOCTYPE html>
<html lang="zh-CN">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/duck-icon.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>天气鸭 - Weather Duck</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

**创建 src/main.tsx**:

```typescript
import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
```

**创建 src/App.tsx**:

```typescript
import React from 'react';

function App() {
  return (
    <div className="app-container">
      <header>
        <h1>天气鸭</h1>
        <p>Weather Duck - 项目初始化成功</p>
      </header>
      <main>
        <p>开发环境已就绪，准备开始开发！</p>
      </main>
    </div>
  );
}

export default App;
```

**创建 src/index.css**:

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  * { @apply box-border; }
  body {
    @apply min-h-screen flex justify-center items-center font-sans;
    @apply bg-gradient-to-br from-slate-100 to-indigo-100;
  }
}

@layer components {
  .app-container {
    @apply text-center p-8 bg-white/90 rounded-2xl shadow-2xl;
  }
}
```

**验收标准**:
- [ ] 基础文件创建完成
- [ ] 代码可以成功编译
- [ ] 开发服务器可以正常启动

---

### 步骤 1.8：验证开发环境

**适用规范**: 
- `.qoder/rules/requirements-spec.zh-CN.md` - [规则 10] 确保代码成功编译 (第170行)
  - 交付前检查语法，解决所有编译错误，测试构建过程

**验证步骤**:

```powershell
# 1. 类型检查
npm run typecheck

# 2. 代码规范检查
npm run lint

# 3. 启动开发服务器（Web版）
npm run dev:web

# 4. 在浏览器访问 http://localhost:3000
# 应该看到"天气鸭 - 项目初始化成功"的页面
```

**验收标准**:
- [ ] TypeScript 类型检查通过
- [ ] ESLint 检查通过
- [ ] 开发服务器正常启动
- [ ] 页面正常显示
- [ ] HMR 热重载功能正常

---

### 步骤 1.9：配置版本控制

**适用规范**: 
- `.qoder/rules/workflow-spec.zh-CN.md` - [规则 1] 变更日志管理 (第19行)
  - 在 CHANGELOG.md 中记录所有重要变更，包含版本号、日期和变更描述

**初始化 Git 仓库**:

```powershell
git init
git add .
git commit -m "feat: 项目初始化完成

- 配置 Electron + Vite + React 开发环境
- 设置 TypeScript 和代码规范工具
- 创建基础项目结构
- 配置环境变量管理

Refs: #1"
```

**创建 CHANGELOG.md**:

```markdown
# 变更日志

本文件记录项目的所有重要变更。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [未发布]

### Added
- 初始化项目结构
- 配置 Electron + Vite + React 开发环境
- 设置 TypeScript 类型检查
- 配置 ESLint 和 Prettier 代码规范
- 添加环境变量管理
- 创建基础组件和样式

## [0.1.0] - 2024-01-01

### Added
- 项目创建
```

**验收标准**:
- [ ] Git 仓库初始化完成
- [ ] 提交信息遵循 Conventional Commits 规范
- [ ] CHANGELOG.md 创建并记录初始变更

---

## 📊 阶段验收清单

### 环境配置验收
- [ ] Node.js 和 npm 版本正确（Node.js >= 18）
- [ ] 所有依赖安装成功
- [ ] TypeScript 配置正确
- [ ] Vite 配置正确
- [ ] 环境变量配置正确

### 代码质量验收
- [ ] ESLint 检查通过
- [ ] Prettier 格式化正常
- [ ] TypeScript 编译无错误
- [ ] 所有文件命名符合规范

### 功能验收
- [ ] Web 开发服务器可以启动
- [ ] 页面可以正常访问
- [ ] HMR 热重载功能正常
- [ ] 基础样式显示正确

### 规范符合性验收
- [ ] 遵循开发需求规范的所有关键规则
- [ ] 遵循命名约定规范
- [ ] 遵循安全规范的配置管理要求
- [ ] Git 提交信息符合工作流规范

---

## 🔍 常见问题解决

### Q1: npm install 失败
**解决方案**:
```powershell
# 清除缓存
npm cache clean --force

# 删除 node_modules
Remove-Item -Recurse -Force node_modules

# 重新安装
npm install
```

### Q2: TypeScript 编译错误
**解决方案**:
- 检查 tsconfig.json 配置
- 确保所有 @types 依赖已安装
- 运行 `npm run typecheck` 查看详细错误

### Q3: 环境变量不生效
**解决方案**:
- 确保 .env 文件在项目根目录
- 重启开发服务器
- 检查变量名是否以 VITE_ 开头

---

## 📝 下一步

完成本阶段后，请继续阅读：
- **阶段2：UI 框架和样式系统搭建**
- **阶段3：核心功能实现**

### 步骤 1.10：集成样式系统与测试栈（与阶段2保持一致）

**适用规范**: 
- `.qoder/rules/requirements-spec.zh-CN.md` - [规则 3] 最小化新增依赖
- `.qoder/rules/requirements-spec.zh-CN.md` - [规则 10] 确保代码成功编译
- `.qoder/rules/requirements-spec.zh-CN.md` - [规则 13] 只使用真实存在的库

**使用 cnpm 安装依赖**:

```powershell
cnpm install -D tailwindcss@3 postcss autoprefixer ; npx tailwindcss init -p
cnpm install clsx tailwind-merge
cnpm install lucide-react
cnpm install -D vitest @testing-library/react @testing-library/jest-dom @testing-library/user-event jsdom ; cnpm install -D @testing-library/dom
```

**新增/更新文件**:
- `tailwind.config.js`：启用 `darkMode: 'class'`，`content` 指向 `index.html` 与 `src/**/*.{ts,tsx}`
- `postcss.config.js`：注册 `tailwindcss` 与 `autoprefixer`
- `src/index.css`：加入 `@tailwind base/components/utilities` 与自定义 `@layer`
- `vitest.config.ts` 与 `src/test/setup.ts`：启用 `jsdom` 与覆盖率

**验证命令**:

```powershell
npm run typecheck ; npm run lint ; npm run test ; npm run dev:web
```

**说明**:
- 以上安装命令使用 `cnpm`，其余运行脚本与阶段1保持一致；如本机未安装 `cnpm`，请先安装或切换为 `npm`

---

**文档版本**: v1.2  
**更新日期**: 2025-11-13  
**维护团队**: 天气鸭开发团队  
**修改人**: Trae Code Assistant

**版本历史**:
- v1.2 (2025-11-13): 同步阶段1与阶段2最新规范，采用 Tailwind CSS；统一依赖安装为 cnpm；更新基础样式文件为 Tailwind 指令；保持测试与目录规范一致
- v1.1 (2025-11-13): 新增“步骤 1.10”，统一安装命令为 cnpm，补充测试依赖 @testing-library/dom；完善验证命令
- v1.0 (2024-01-01): 初始版本

---

---

## 📚 规范文件快速参考

本文档引用的所有规范条款详见:

| 规范类别 | 文件路径 | 规则编号 | 起始行 |
|---------|---------|---------|--------|
| 开发需求规范 | `.qoder/rules/requirements-spec.zh-CN.md` | 规则 1 | 第20行 |
| 开发需求规范 | `.qoder/rules/requirements-spec.zh-CN.md` | 规则 3 | 第69行 |
| 开发需求规范 | `.qoder/rules/requirements-spec.zh-CN.md` | 规则 10 | 第171行 |
| 开发需求规范 | `.qoder/rules/requirements-spec.zh-CN.md` | 规则 13 | 第199行 |
| 工作流规范 | `.qoder/rules/workflow-spec.zh-CN.md` | 规则 2 | 第50行 |
| 工作流规范 | `.qoder/rules/workflow-spec.zh-CN.md` | 规则 6 | 第141行 |
| 工作流规范 | `.qoder/rules/workflow-spec.zh-CN.md` | 规则 10 | 第229行 |
| 命名约定 | `.qoder/rules/naming-conventions.zh-CN.md` | 约定 5 | 第154行 |
| 命名约定 | `.qoder/rules/naming-conventions.zh-CN.md` | 约定 9 | 第277行 |
| 安全规范 | `.qoder/rules/security-spec.zh-CN.md` | 规则 4 | 第128行 |
| 安全规范 | `.qoder/rules/security-spec.zh-CN.md` | 规则 8 | 第231行 |
| 技术方案 | `技术方案v1.0.md` | 架构选型 | 第7-83行 |
| 技术方案 | `技术方案v1.0.md` | 技术栈 | 第124-285行 |

**使用方法**:
1. 在IDE中使用 `Ctrl+G` (或 `Cmd+G`) 跳转到指定行号
2. 或在Qoder中使用 `@文件路径` 直接打开规范文件

---

**文档版本**: v1.0  
**更新日期**: 2024-01-01  
**维护团队**: 天气鸭开发团队
