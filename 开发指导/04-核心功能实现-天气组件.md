# 天气鸭项目开发指导 - 阶段4：核心功能实现 - 天气组件

## 📋 阶段概述

本阶段将实现天气显示相关的核心组件，包括天气卡片、预报列表、天气动画等功能模块。

---

## 🎯 核心目标

- ✅ 创建天气卡片组件（需求1、需求2）
- ✅ 实现天气预报组件（需求3、需求4）
- ✅ 集成天气动画系统（需求2）
- ✅ 创建天气图标映射

---

## 📖 适用规范清单

### 核心规范
- **开发需求规范**
  - [规则 2] 复用现有代码和API
  - [规则 11] 有条理地分步骤实现
  - [规则 12] 尊重项目命名约定

- **命名约定**
  - [约定 2] 函数命名 (camelCase)
  - [约定 6] 组件命名 (PascalCase)
  - [约定 8] Props接口命名 (PascalCase + Props后缀)

### 质量规范
- **测试规范**
  - [规则 1] 测试完整性
  - [规则 3] 可维护性

- **错误处理规范**
  - [规则 3] 用户友好的错误提示

---

## 🚀 实施步骤

### 步骤 4.1：创建天气图标映射

**适用规范**: 命名约定 [约定 4] 常量命名

**创建 src/constants/weather-icons.ts**:

```typescript
import type { IconName } from '@/components/Icon';

/**
 * 和风天气图标代码到Lucide图标的映射
 * 遵循命名约定：常量使用 UPPER_SNAKE_CASE
 */

export const WEATHER_ICON_MAP: Record<string, IconName> = {
  // 晴天
  '100': 'Sun',              // 晴
  '101': 'CloudSun',         // 多云
  '102': 'Cloud',            // 少云
  '103': 'Cloud',            // 晴间多云
  
  // 阴天
  '104': 'Cloud',            // 阴
  
  // 雨天
  '300': 'CloudDrizzle',     // 阵雨
  '301': 'CloudRain',        // 强阵雨
  '302': 'CloudLightning',   // 雷阵雨
  '303': 'CloudLightning',   // 强雷阵雨
  '304': 'CloudRainWind',    // 雷阵雨伴有冰雹
  '305': 'CloudDrizzle',     // 小雨
  '306': 'CloudRain',        // 中雨
  '307': 'CloudRain',        // 大雨
  '308': 'CloudRain',        // 极端降雨
  '309': 'CloudDrizzle',     // 毛毛雨/细雨
  '310': 'CloudRain',        // 暴雨
  '311': 'CloudRain',        // 大暴雨
  '312': 'CloudRain',        // 特大暴雨
  '313': 'CloudRainWind',    // 冻雨
  '314': 'CloudDrizzle',     // 小到中雨
  '315': 'CloudRain',        // 中到大雨
  '316': 'CloudRain',        // 大到暴雨
  '317': 'CloudRain',        // 暴雨到大暴雨
  '318': 'CloudRain',        // 大暴雨到特大暴雨
  
  // 雪天
  '400': 'CloudSnow',        // 阵雪
  '401': 'Snowflake',        // 强阵雪
  '402': 'CloudSnow',        // 雨夹雪
  '403': 'CloudSnow',        // 强雨夹雪
  '404': 'CloudDrizzle',     // 雨雪天气
  '405': 'CloudSnow',        // 小雪
  '406': 'CloudSnow',        // 中雪
  '407': 'Snowflake',        // 大雪
  '408': 'Snowflake',        // 暴雪
  '409': 'CloudSnow',        // 小到中雪
  '410': 'CloudSnow',        // 中到大雪
  '456': 'CloudSnow',        // 阵雨夹雪
  '457': 'CloudSnow',        // 强阵雨夹雪
  
  // 雾霾
  '500': 'CloudFog',         // 薄雾
  '501': 'CloudFog',         // 雾
  '502': 'CloudFog',         // 霾
  '503': 'Wind',             // 扬沙
  '504': 'Wind',             // 浮尘
  '507': 'Wind',             // 沙尘暴
  '508': 'Wind',             // 强沙尘暴
  '509': 'CloudFog',         // 浓雾
  '510': 'CloudFog',         // 强浓雾
  '511': 'CloudFog',         // 中度霾
  '512': 'CloudFog',         // 重度霾
  '513': 'CloudFog',         // 严重霾
  '514': 'CloudFog',         // 大雾
  '515': 'CloudFog',         // 特强浓雾
  
  // 其他
  '900': 'Thermometer',      // 热
  '901': 'Snowflake',        // 冷
  '999': 'HelpCircle',       // 未知
} as const;

/**
 * 根据天气代码获取图标名称
 */
export function getWeatherIcon(code: string): IconName {
  return WEATHER_ICON_MAP[code] || 'HelpCircle';
}

/**
 * 天气描述颜色映射
 */
export const WEATHER_COLOR_MAP: Record<string, string> = {
  // 晴天系列 - 金色
  '100': '#FFB300',
  '101': '#FFA000',
  '102': '#FF8F00',
  '103': '#FF6F00',
  
  // 阴天系列 - 灰色
  '104': '#78909C',
  
  // 雨天系列 - 蓝色
  '300': '#42A5F5',
  '301': '#1E88E5',
  '302': '#1565C0',
  '303': '#0D47A1',
  '305': '#64B5F6',
  '306': '#42A5F5',
  '307': '#1E88E5',
  '308': '#1565C0',
  '310': '#1565C0',
  '311': '#0D47A1',
  '312': '#0D47A1',
  
  // 雪天系列 - 淡蓝/白色
  '400': '#E3F2FD',
  '405': '#BBDEFB',
  '406': '#90CAF9',
  '407': '#64B5F6',
  '408': '#42A5F5',
  
  // 雾霾系列 - 灰褐色
  '500': '#B0BEC5',
  '501': '#90A4AE',
  '502': '#78909C',
  '509': '#607D8B',
  '510': '#546E7A',
} as const;

/**
 * 根据天气代码获取颜色
 */
export function getWeatherColor(code: string): string {
  return WEATHER_COLOR_MAP[code] || '#3B82F6';
}
```

**验收标准**:
- [ ] 常量使用 UPPER_SNAKE_CASE
- [ ] 图标映射完整
- [ ] 颜色映射合理

---

### 步骤 4.2：创建天气卡片组件

**适用规范**: 命名约定 [约定 6] 组件命名、[约定 8] Props接口命名

**创建 src/components/Weather/WeatherCard.tsx**:

```typescript
import React from 'react';
import { Card } from '@/components/Card';
import { Icon } from '@/components/Icon';
import { getWeatherIcon, getWeatherColor } from '@/constants/weather-icons';
import type { CurrentWeather } from '@/types/weather';
import { cn } from '@/utils/cn';

export interface WeatherCardProps {
  weather: CurrentWeather;
  cityName?: string;
  className?: string;
  onClick?: () => void;
}

/**
 * WeatherCard 天气卡片组件
 * 显示当前天气的核心信息
 * 遵循命名约定：组件名 PascalCase，Props接口 PascalCase + Props后缀
 */
export const WeatherCard: React.FC<WeatherCardProps> = ({
  weather,
  cityName,
  className,
  onClick,
}) => {
  const iconName = getWeatherIcon(weather.icon);
  const weatherColor = getWeatherColor(weather.icon);

  return (
    <Card 
      variant="glass" 
      className={cn('relative overflow-hidden', className)}
      onClick={onClick}
    >
      {/* 背景装饰 */}
      <div 
        className="absolute top-0 right-0 w-32 h-32 rounded-full blur-3xl opacity-20"
        style={{ background: weatherColor }}
      />

      {/* 城市名称 */}
      {cityName && (
        <div className="flex items-center justify-between mb-3">
          <h3 className="text-lg font-bold text-blue-900">
            <Icon name="MapPin" size={18} className="inline mr-1" />
            {cityName}
          </h3>
        </div>
      )}

      {/* 主要天气信息 */}
      <div className="flex items-center justify-between mb-4">
        {/* 温度 */}
        <div>
          <div className="text-6xl font-bold text-blue-900">
            {Math.round(Number(weather.temp))}°
          </div>
          <div className="text-sm text-blue-600 mt-1">
            体感 {Math.round(Number(weather.feelsLike))}°
          </div>
        </div>

        {/* 天气图标和描述 */}
        <div className="text-center">
          <Icon 
            name={iconName} 
            size={64} 
            color={weatherColor}
            className="mb-2"
          />
          <div className="text-lg font-medium text-blue-900">
            {weather.text}
          </div>
        </div>
      </div>

      {/* 详细信息 */}
      <div className="grid grid-cols-4 gap-2 pt-3 border-t border-blue-200">
        <div className="text-center">
          <Icon name="Droplets" size={16} color="#64B5F6" className="mx-auto mb-1" />
          <div className="text-xs text-blue-600">湿度</div>
          <div className="text-sm font-bold text-blue-900">{weather.humidity}%</div>
        </div>

        <div className="text-center">
          <Icon name="Wind" size={16} color="#64B5F6" className="mx-auto mb-1" />
          <div className="text-xs text-blue-600">风速</div>
          <div className="text-sm font-bold text-blue-900">{weather.windSpeed}km/h</div>
        </div>

        <div className="text-center">
          <Icon name="Gauge" size={16} color="#64B5F6" className="mx-auto mb-1" />
          <div className="text-xs text-blue-600">气压</div>
          <div className="text-sm font-bold text-blue-900">{weather.pressure}hPa</div>
        </div>

        <div className="text-center">
          <Icon name="Eye" size={16} color="#64B5F6" className="mx-auto mb-1" />
          <div className="text-xs text-blue-600">能见度</div>
          <div className="text-sm font-bold text-blue-900">{weather.vis}km</div>
        </div>
      </div>

      {/* 更新时间 */}
      <div className="mt-3 text-xs text-blue-500 text-center">
        更新于 {new Date(weather.obsTime).toLocaleTimeString('zh-CN', {
          hour: '2-digit',
          minute: '2-digit'
        })}
      </div>
    </Card>
  );
};
```

**验收标准**:
- [ ] 组件命名符合规范
- [ ] Props接口命名正确
- [ ] 天气信息显示完整
- [ ] 视觉效果符合设计

---

### 步骤 4.3：创建天气预报列表组件

**适用规范**: 开发需求规范 [规则 2] 复用现有代码和API

**创建 src/components/Weather/ForecastList.tsx**:

```typescript
import React from 'react';
import { Card } from '@/components/Card';
import { Icon } from '@/components/Icon';
import { getWeatherIcon, getWeatherColor } from '@/constants/weather-icons';
import type { WeatherForecast } from '@/types/weather';
import { cn } from '@/utils/cn';

export interface ForecastListProps {
  forecasts: WeatherForecast[];
  className?: string;
}

/**
 * ForecastList 天气预报列表组件
 * 显示未来7天的天气预报
 */
export const ForecastList: React.FC<ForecastListProps> = ({
  forecasts,
  className,
}) => {
  /**
   * 格式化日期显示
   * 遵循命名约定：函数名 camelCase，动词开头
   */
  const formatDate = (dateStr: string): string => {
    const date = new Date(dateStr);
    const today = new Date();
    const tomorrow = new Date(today);
    tomorrow.setDate(tomorrow.getDate() + 1);

    if (date.toDateString() === today.toDateString()) {
      return '今天';
    }
    if (date.toDateString() === tomorrow.toDateString()) {
      return '明天';
    }

    const weekdays = ['周日', '周一', '周二', '周三', '周四', '周五', '周六'];
    return weekdays[date.getDay()];
  };

  return (
    <div className={cn('space-y-2', className)}>
      <h3 className="text-lg font-bold text-blue-900 mb-3">
        <Icon name="Calendar" size={20} className="inline mr-2" />
        7天预报
      </h3>

      {forecasts.map((forecast, index) => {
        const dayIcon = getWeatherIcon(forecast.iconDay);
        const dayColor = getWeatherColor(forecast.iconDay);

        return (
          <Card 
            key={forecast.fxDate}
            variant="glass"
            hoverable
            className="transition-all duration-300"
          >
            <div className="flex items-center justify-between">
              {/* 日期 */}
              <div className="flex-1">
                <div className="font-bold text-blue-900">
                  {formatDate(forecast.fxDate)}
                </div>
                <div className="text-xs text-blue-600">
                  {new Date(forecast.fxDate).toLocaleDateString('zh-CN', {
                    month: '2-digit',
                    day: '2-digit'
                  })}
                </div>
              </div>

              {/* 天气图标和描述 */}
              <div className="flex-1 text-center">
                <Icon name={dayIcon} size={32} color={dayColor} className="mx-auto" />
                <div className="text-sm text-blue-900 mt-1">
                  {forecast.textDay}
                </div>
              </div>

              {/* 温度范围 */}
              <div className="flex-1 text-right">
                <div className="text-lg font-bold text-blue-900">
                  {Math.round(Number(forecast.tempMax))}°
                </div>
                <div className="text-sm text-blue-600">
                  {Math.round(Number(forecast.tempMin))}°
                </div>
              </div>

              {/* 降水概率 */}
              {Number(forecast.precip) > 0 && (
                <div className="ml-3 text-blue-500">
                  <Icon name="Droplets" size={16} className="inline" />
                  <span className="text-xs ml-1">{forecast.precip}mm</span>
                </div>
              )}
            </div>
          </Card>
        );
      })}
    </div>
  );
};
```

**验收标准**:
- [ ] 预报列表显示正确
- [ ] 日期格式化正确
- [ ] 图标和颜色匹配
- [ ] 交互动画流畅

---

### 步骤 4.4：创建24小时预报组件

**适用规范**: 命名约定 [约定 2] 函数命名

**创建 src/components/Weather/HourlyForecast.tsx**:

```typescript
import React, { useRef } from 'react';
import { Card } from '@/components/Card';
import { Icon } from '@/components/Icon';
import { getWeatherIcon, getWeatherColor } from '@/constants/weather-icons';
import type { HourlyWeather } from '@/types/weather';
import { cn } from '@/utils/cn';

export interface HourlyForecastProps {
  hourlyData: HourlyWeather[];
  className?: string;
}

/**
 * HourlyForecast 24小时预报组件
 * 横向滚动显示逐小时天气
 */
export const HourlyForecast: React.FC<HourlyForecastProps> = ({
  hourlyData,
  className,
}) => {
  const scrollRef = useRef<HTMLDivElement>(null);

  /**
   * 格式化小时显示
   */
  const formatHour = (timeStr: string): string => {
    const date = new Date(timeStr);
    const now = new Date();

    if (date.getHours() === now.getHours() && 
        date.getDate() === now.getDate()) {
      return '现在';
    }

    return date.toLocaleTimeString('zh-CN', {
      hour: '2-digit',
      minute: '2-digit',
      hour12: false
    });
  };

  return (
    <div className={cn(className)}>
      <h3 className="text-lg font-bold text-blue-900 mb-3">
        <Icon name="Clock" size={20} className="inline mr-2" />
        24小时预报
      </h3>

      <div 
        ref={scrollRef}
        className="flex space-x-3 overflow-x-auto pb-2 scrollbar-hide"
        style={{ scrollBehavior: 'smooth' }}
      >
        {hourlyData.map((hour, index) => {
          const icon = getWeatherIcon(hour.icon);
          const color = getWeatherColor(hour.icon);

          return (
            <Card
              key={hour.fxTime}
              variant="glass"
              padding="sm"
              hoverable
              className="flex-shrink-0 w-20 text-center"
            >
              {/* 时间 */}
              <div className="text-xs text-blue-600 mb-2 font-medium">
                {formatHour(hour.fxTime)}
              </div>

              {/* 天气图标 */}
              <Icon 
                name={icon} 
                size={28} 
                color={color}
                className="mx-auto mb-2"
              />

              {/* 温度 */}
              <div className="text-lg font-bold text-blue-900 mb-1">
                {Math.round(Number(hour.temp))}°
              </div>

              {/* 降水概率 */}
              {Number(hour.pop) > 30 && (
                <div className="text-xs text-blue-500">
                  <Icon name="Droplets" size={12} className="inline" />
                  <span className="ml-1">{hour.pop}%</span>
                </div>
              )}

              {/* 风向风速 */}
              <div className="text-xs text-blue-500 mt-1">
                {hour.windDir}
                <br />
                {hour.windScale}级
              </div>
            </Card>
          );
        })}
      </div>

      {/* 添加自定义滚动条样式 */}
      <style>{`
        .scrollbar-hide::-webkit-scrollbar {
          display: none;
        }
        .scrollbar-hide {
          -ms-overflow-style: none;
          scrollbar-width: none;
        }
      `}</style>
    </div>
  );
};
```

**验收标准**:
- [ ] 横向滚动正常
- [ ] 时间格式化正确
- [ ] 降水概率显示合理
- [ ] 滚动条样式美观

---

### 步骤 4.5：创建天气详情组件

**适用规范**: 开发需求规范 [规则 11] 有条理地分步骤实现

**创建 src/components/Weather/WeatherDetail.tsx**:

```typescript
import React from 'react';
import { Card } from '@/components/Card';
import { Icon } from '@/components/Icon';
import type { IconName } from '@/components/Icon';
import type { CurrentWeather } from '@/types/weather';
import { cn } from '@/utils/cn';

export interface WeatherDetailProps {
  weather: CurrentWeather;
  className?: string;
}

interface DetailItem {
  icon: IconName;
  label: string;
  value: string;
  unit: string;
  color: string;
}

/**
 * WeatherDetail 天气详情组件
 * 显示完整的天气指标信息
 */
export const WeatherDetail: React.FC<WeatherDetailProps> = ({
  weather,
  className,
}) => {
  const details: DetailItem[] = [
    {
      icon: 'Thermometer',
      label: '体感温度',
      value: weather.feelsLike,
      unit: '°C',
      color: '#FF6B6B',
    },
    {
      icon: 'Droplets',
      label: '相对湿度',
      value: weather.humidity,
      unit: '%',
      color: '#4ECDC4',
    },
    {
      icon: 'Wind',
      label: '风速',
      value: weather.windSpeed,
      unit: 'km/h',
      color: '#95E1D3',
    },
    {
      icon: 'Navigation',
      label: '风向',
      value: weather.windDir,
      unit: '',
      color: '#FFD93D',
    },
    {
      icon: 'Gauge',
      label: '气压',
      value: weather.pressure,
      unit: 'hPa',
      color: '#6C5CE7',
    },
    {
      icon: 'Eye',
      label: '能见度',
      value: weather.vis,
      unit: 'km',
      color: '#74B9FF',
    },
    {
      icon: 'CloudRain',
      label: '降水量',
      value: weather.precip,
      unit: 'mm',
      color: '#0984E3',
    },
    {
      icon: 'Cloud',
      label: '云量',
      value: weather.cloud,
      unit: '%',
      color: '#B2BEC3',
    },
  ];

  return (
    <div className={cn(className)}>
      <h3 className="text-lg font-bold text-blue-900 mb-3">
        <Icon name="Info" size={20} className="inline mr-2" />
        天气详情
      </h3>

      <div className="grid grid-cols-2 gap-3">
        {details.map((detail) => (
          <Card
            key={detail.label}
            variant="glass"
            hoverable
            padding="md"
          >
            <div className="flex items-center space-x-3">
              <div 
                className="flex-shrink-0 w-10 h-10 rounded-full flex items-center justify-center"
                style={{ background: `${detail.color}20` }}
              >
                <Icon name={detail.icon} size={20} color={detail.color} />
              </div>

              <div className="flex-1">
                <div className="text-xs text-blue-600 mb-1">
                  {detail.label}
                </div>
                <div className="text-lg font-bold text-blue-900">
                  {detail.value}
                  <span className="text-sm text-blue-600 ml-1">
                    {detail.unit}
                  </span>
                </div>
              </div>
            </div>
          </Card>
        ))}
      </div>
    </div>
  );
};
```

**验收标准**:
- [ ] 所有天气指标显示完整
- [ ] 图标和颜色美观
- [ ] 布局响应式
- [ ] 卡片交互正常

---

### 步骤 4.6：创建组件统一导出

**适用规范**: 开发需求规范 [规则 2] 复用现有代码和API

**创建 src/components/Weather/index.ts**:

```typescript
/**
 * 天气组件统一导出
 */

export { WeatherCard } from './WeatherCard';
export type { WeatherCardProps } from './WeatherCard';

export { ForecastList } from './ForecastList';
export type { ForecastListProps } from './ForecastList';

export { HourlyForecast } from './HourlyForecast';
export type { HourlyForecastProps } from './HourlyForecast';

export { WeatherDetail } from './WeatherDetail';
export type { WeatherDetailProps } from './WeatherDetail';
```

**更新 src/components/index.ts**:

```typescript
// ... 现有导出 ...

export * from './Weather';
```

**验收标准**:
- [ ] 所有组件正确导出
- [ ] 类型定义正确导出
- [ ] 导入路径简洁

---

## 📊 阶段验收清单

### 组件功能验收
- [ ] WeatherCard 显示正确
- [ ] ForecastList 显示正确
- [ ] HourlyForecast 显示正确
- [ ] WeatherDetail 显示正确

### 命名规范验收
- [ ] 组件名使用 PascalCase
- [ ] Props接口使用 PascalCase + Props后缀
- [ ] 函数名使用 camelCase
- [ ] 常量使用 UPPER_SNAKE_CASE

### 视觉效果验收
- [ ] 天气图标正确映射
- [ ] 颜色主题协调统一
- [ ] 动画效果流畅
- [ ] 毛玻璃效果美观

### 代码质量验收
- [ ] 组件可复用性好
- [ ] 类型定义完整
- [ ] 注释清晰
- [ ] 无TypeScript错误

---

## 📝 下一步

完成本阶段后，请继续阅读：
- **阶段5：核心功能实现 - 时钟和报时**
- **阶段6：平台适配和Electron集成**

---

**文档版本**: v1.0  
**更新日期**: 2024-01-01  
**维护团队**: 天气鸭开发团队
