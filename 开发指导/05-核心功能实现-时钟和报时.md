# 天气鸭项目开发指导 - 阶段5：核心功能实现 - 时钟和报时
> 文档版本: v1.1  | 更新日期: 2025-11-13

## 📋 阶段概述

本阶段将实现时钟显示和布谷鸟报时功能，这是"天气鸭"项目的核心特色功能之一。

---

## 🎯 核心目标

- ✅ 创建数字时钟组件（需求13）
- ✅ 创建模拟时钟组件（需求13）
- ✅ 实现布谷鸟报时功能（需求12）
- ✅ 集成音频播放系统

---

## 标准章节补充

### 概述
- 本阶段聚焦“时钟显示与整点报时”。示例代码以现有组件为基础，提供可直接运行的数字时钟展示与报时逻辑示例，并对音频播放安全与错误恢复进行说明。

### 接口定义
- 前端接口（Hooks/组件约定）：
  - `useClock`：提供当前时间数据（小时/分钟/秒、格式化字符串）
  - `useHourlyChime`：在整点触发报时回调
  - `AudioManager`：基于 Web Audio API 的音频播放管理（主音量、播放、释放资源）
- 以上为建议抽象；示例代码提供无需新文件的可运行版本。

### 请求参数
- 无外部API请求参数；报时逻辑可配置时间段（如8:00-22:00）、音量范围（0-1）。
- 运行条件：浏览器环境需支持 Web Audio API（如`AudioContext`）。

### 响应格式
- 时钟数据结构：`{ hours, minutes, seconds, formattedTime, formattedDate, isPM }`。
- 报时回调：接收当前小时数并执行播放逻辑；错误情况下需优雅降级并给出提示。

### 错误代码
- 不涉及外部服务错误码；但需遵守项目错误处理规范：
  - 音频上下文初始化失败时记录错误并放弃播放
  - 用户未交互导致`AudioContext`挂起时的恢复处理

### 示例代码（可直接运行）
```tsx
import React, { useEffect, useMemo, useRef, useState } from 'react'
import { Card } from '@/components/Card'
import { Icon } from '@/components/Icon'

export const ClockDemo: React.FC = () => {
  const [now, setNow] = useState(new Date())
  const [isChiming, setIsChiming] = useState(false)
  const audioCtxRef = useRef<AudioContext | null>(null)
  const gainRef = useRef<GainNode | null>(null)

  useEffect(() => {
    const timer = setInterval(() => setNow(new Date()), 1000)
    return () => clearInterval(timer)
  }, [])

  useEffect(() => {
    const h = now.getHours()
    const m = now.getMinutes()
    const s = now.getSeconds()
    if (h >= 8 && h < 22 && m === 0 && s === 0) {
      setIsChiming(true)
      const times = h > 12 ? h - 12 : h
      playCuckoo(times).finally(() => setIsChiming(false))
    }
  }, [now])

  const playCuckoo = async (times: number) => {
    try {
      if (!audioCtxRef.current) {
        const Ctx = (window as any).AudioContext || (window as any).webkitAudioContext
        audioCtxRef.current = Ctx ? new Ctx() : null
        if (!audioCtxRef.current) return
        gainRef.current = audioCtxRef.current.createGain()
        gainRef.current.gain.value = 0.5
        gainRef.current.connect(audioCtxRef.current.destination)
      }
      if (audioCtxRef.current.state === 'suspended') {
        await audioCtxRef.current.resume()
      }
      for (let i = 0; i < times; i++) {
        await beep()
        if (i < times - 1) await sleep(800)
      }
    } catch (e) {
    }
  }

  const beep = async () => {
    if (!audioCtxRef.current || !gainRef.current) return
    const osc = audioCtxRef.current.createOscillator()
    const g = audioCtxRef.current.createGain()
    osc.connect(g)
    g.connect(gainRef.current)
    osc.type = 'sine'
    osc.frequency.setValueAtTime(800, audioCtxRef.current.currentTime)
    osc.frequency.setValueAtTime(600, audioCtxRef.current.currentTime + 0.15)
    g.gain.setValueAtTime(0, audioCtxRef.current.currentTime)
    g.gain.linearRampToValueAtTime(0.3, audioCtxRef.current.currentTime + 0.05)
    g.gain.linearRampToValueAtTime(0, audioCtxRef.current.currentTime + 0.3)
    osc.start()
    osc.stop(audioCtxRef.current.currentTime + 0.3)
    await sleep(300)
  }

  const sleep = (ms: number) => new Promise(r => setTimeout(r, ms))

  const formattedDate = useMemo(() => now.toLocaleDateString('zh-CN', {
    year: 'numeric', month: 'long', day: 'numeric', weekday: 'long'
  }), [now])
  const hh = String(now.getHours()).padStart(2, '0')
  const mm = String(now.getMinutes()).padStart(2, '0')
  const ss = String(now.getSeconds()).padStart(2, '0')

  return (
    <Card variant="glass" padding="lg" className="text-center">
      <div className="text-blue-600 text-sm mb-2">{formattedDate}</div>
      <div className="font-mono text-6xl font-bold text-blue-900 tracking-wider">
        {hh}<span className="animate-pulse">:</span>{mm}
        <span className="animate-pulse">:</span>
        <span className="text-4xl text-blue-600">{ss}</span>
      </div>
      <div className="mt-4 h-24">
        {isChiming && (
          <div className="relative w-24 h-24 mx-auto">
            <div className="text-6xl animate-bounce">🦆</div>
            <div className="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2">
              <div className="w-24 h-24 rounded-full border-4 border-blue-400 animate-ping opacity-75" />
            </div>
            <div className="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2">
              <div className="w-32 h-32 rounded-full border-4 border-blue-300 animate-ping opacity-50" />
            </div>
          </div>
        )}
      </div>
    </Card>
  )
}
```

---

## 📖 适用规范清单

### 核心规范

#### 开发需求规范
文件路径: `.qoder/rules/requirements-spec.zh-CN.md`

- **[规则 1] 生成完整可运行代码** → 第20行
- **[规则 11] 严格遵循提供的示例** → 第183行
- **[规则 12] 尊重项目命名约定** → 第191行

#### 命名约定
文件路径: `.qoder/rules/naming-conventions.zh-CN.md`

- **[约定 1] 变量命名** → 第20行 (camelCase)
- **[约定 2] 函数命名** → 第54行 (camelCase, 动词开头)
- **[约定 3] 类命名** → 第90行 (PascalCase)
- **[约定 6] 组件命名** → 第190行 (PascalCase)

### 质量规范

#### 错误处理规范
文件路径: `.qoder/rules/error-handling-spec.zh-CN.md`

- **[规则 3] 用户友好的错误提示** → 第116行
- **[规则 6] 错误恢复策略** → 第216行

### 需求验收参考

#### 需求文档
文件路径: `requirements.md`

- **需求12: 整点布谷鸟报时功能** → 第158-171行
  - 整点播放布谷鸟音效,次数对应小时数
  - 报时时间段设置(默认8:00-22:00)
  - 布谷鸟动画(从时钟弹出)
  - 音量控制和静音模式
  - 后台运行支持

- **需求13: 实时时钟显示功能** → 第172-186行
  - 数字时钟和模拟时钟两种样式
  - 24小时制/12小时制切换
  - 时针、分针、秒针平滑移动
  - 多种时钟主题和颜色
  - 与布谷鸟功能集成

- **需求14: 音频系统和音效管理** → 第187-200行
  - 支持MP3、WAV、OGG格式
  - 主音量和分类音量控制
  - 多种布谷鸟声音选项
  - 音效预览功能

---

## 🚀 实施步骤

### 步骤 5.1：创建时钟Hooks

**适用规范**: 
- 命名约定 [约定 2] 函数命名 (camelCase) → 第54行
  - Hook使用 use + camelCase 命名（useClock、useHourlyChime）
- 开发需求规范 [规则 1] 生成完整可运行代码 → 第20行
  - 提供完整的Hook实现，无占位符，包含所有必要的类型定义
- 命名约定 [约定 10] 类型/接口命名 (PascalCase) → 第315行
  - ClockTime、HourlyChimeOptions接口使用PascalCase
- 开发需求规范 [规则 2] 复用现有代码和API → 第48行
  - 使用React原生Hooks（useState、useEffect、useRef）

**创建 src/hooks/use-clock.ts**:

```typescript
import { useState, useEffect } from 'react';

export interface ClockTime {
  hours: number;
  minutes: number;
  seconds: number;
  formattedTime: string;
  formattedDate: string;
  isPM: boolean;
}

/**
 * useClock Hook
 * 提供实时时钟数据
 * 遵循命名约定：Hook使用 use + camelCase
 */
export function useClock(updateInterval: number = 1000): ClockTime {
  const [time, setTime] = useState<ClockTime>(getCurrentTime());

  useEffect(() => {
    const timer = setInterval(() => {
      setTime(getCurrentTime());
    }, updateInterval);

    return () => clearInterval(timer);
  }, [updateInterval]);

  return time;
}

/**
 * 获取当前时间信息
 * 遵循命名约定：函数名 camelCase，动词开头
 */
function getCurrentTime(): ClockTime {
  const now = new Date();
  const hours = now.getHours();
  const minutes = now.getMinutes();
  const seconds = now.getSeconds();

  return {
    hours,
    minutes,
    seconds,
    formattedTime: now.toLocaleTimeString('zh-CN', {
      hour: '2-digit',
      minute: '2-digit',
      second: '2-digit',
      hour12: false,
    }),
    formattedDate: now.toLocaleDateString('zh-CN', {
      year: 'numeric',
      month: 'long',
      day: 'numeric',
      weekday: 'long',
    }),
    isPM: hours >= 12,
  };
}
```

**创建 src/hooks/use-hourly-chime.ts**:

```typescript
import { useEffect, useRef, useState } from 'react';

export interface HourlyChimeOptions {
  enabled: boolean;
  startHour?: number;  // 开始报时的小时（默认8点）
  endHour?: number;    // 结束报时的小时（默认22点）
  onChime?: (hour: number) => void;
}

/**
 * useHourlyChime Hook
 * 实现整点报时功能
 */
export function useHourlyChime(options: HourlyChimeOptions) {
  const { enabled, startHour = 8, endHour = 22, onChime } = options;
  const lastHourRef = useRef<number>(-1);
  const [isChiming, setIsChiming] = useState(false);

  useEffect(() => {
    if (!enabled) return;

    const checkChime = () => {
      const now = new Date();
      const currentHour = now.getHours();
      const currentMinute = now.getMinutes();
      const currentSecond = now.getSeconds();

      // 检查是否在报时时间段内
      if (currentHour < startHour || currentHour >= endHour) {
        return;
      }

      // 整点时刻（小时变化且分钟为0）
      if (
        currentMinute === 0 &&
        currentSecond === 0 &&
        currentHour !== lastHourRef.current
      ) {
        lastHourRef.current = currentHour;
        setIsChiming(true);
        
        // 触发报时回调
        onChime?.(currentHour);

        // 3秒后结束报时动画
        setTimeout(() => {
          setIsChiming(false);
        }, 3000);
      }
    };

    // 每秒检查一次
    const timer = setInterval(checkChime, 1000);

    return () => clearInterval(timer);
  }, [enabled, startHour, endHour, onChime]);

  return { isChiming };
}
```

**验收标准**:
- [ ] Hook命名符合规范（约定2：useClock、useHourlyChime使用use + camelCase）
- [ ] 时钟数据更新正确（规则1：每秒更新，提供完整的时间信息）
- [ ] 整点报时触发准确（需求12：整点00分00秒触发）
- [ ] 代码完整无TODO（规则1：完整实现，无占位符）
- [ ] 接口定义完整（约定10：ClockTime、HourlyChimeOptions使用PascalCase）
- [ ] 复用React原生API（规则2：useState、useEffect、useRef）
- [ ] 时间段控制正确（需求12：默认8:00-22:00报时）

---

### 步骤 5.2：创建数字时钟组件

**适用规范**: 
- 命名约定 [约定 6] 组件命名 (PascalCase) → 第190行
  - DigitalClock组件使用PascalCase
- 命名约定 [约定 10] 类型/接口命名 (PascalCase) → 第315行
  - DigitalClockProps接口使用PascalCase + Props后缀
- 开发需求规范 [规则 2] 复用现有代码和API → 第48行
  - 复用Card组件、useClock Hook和cn工具函数
- 开发需求规范 [规则 1] 生成完整可运行代码 → 第20行
  - 提供完整的数字时钟组件实现
- 开发需求规范 [规则 11] 严格遵循提供的示例 → 第183行
  - 根据需求13实现24小时制显示

**创建 src/components/Clock/DigitalClock.tsx**:

```typescript
import React from 'react';
import { Card } from '@/components/Card';
import { useClock } from '@/hooks/use-clock';
import { cn } from '@/utils/cn';

export interface DigitalClockProps {
  showSeconds?: boolean;
  showDate?: boolean;
  className?: string;
}

/**
 * DigitalClock 数字时钟组件
 * 遵循命名约定：组件名 PascalCase
 */
export const DigitalClock: React.FC<DigitalClockProps> = ({
  showSeconds = true,
  showDate = true,
  className,
}) => {
  const time = useClock();

  return (
    <Card variant="glass" padding="lg" className={cn('text-center', className)}>
      {/* 日期显示 */}
      {showDate && (
        <div className="text-blue-600 text-sm mb-2">
          {time.formattedDate}
        </div>
      )}

      {/* 时间显示 */}
      <div className="font-mono text-6xl font-bold text-blue-900 tracking-wider">
        {String(time.hours).padStart(2, '0')}
        <span className="animate-pulse">:</span>
        {String(time.minutes).padStart(2, '0')}
        {showSeconds && (
          <>
            <span className="animate-pulse">:</span>
            <span className="text-4xl text-blue-600">
              {String(time.seconds).padStart(2, '0')}
            </span>
          </>
        )}
      </div>

      {/* 上午/下午指示 */}
      <div className="mt-2 text-blue-500 text-sm">
        {time.isPM ? '下午' : '上午'}
      </div>
    </Card>
  );
};
```

**验收标准**:
- [ ] 时间显示准确（规则1：HH:MM:SS格式，实时更新）
- [ ] 秒数动画流畅（需求13：秒针平滑移动，使用animate-pulse）
- [ ] 日期格式正确（规则1：年月日星期中文显示）
- [ ] 上午/下午显示正确（需求13：12小时制切换支持）
- [ ] 组件命名符合规范（约定6：DigitalClock使用PascalCase）
- [ ] Props接口定义正确（约定10：DigitalClockProps使用PascalCase + Props后缀）
- [ ] 复用现有组件（规则2：使用Card组件和useClock Hook）
- [ ] 代码可立即运行（规则1：完整实现，包含所有必要的import）

---

### 步骤 5.3：创建模拟时钟组件

**适用规范**: 
- 开发需求规范 [规则 11] 严格遵循提供的示例 → 第183行
  - SVG绘图遵循标准时钟设计规范，实现传统圆形时钟
- 开发需求规范 [规则 9] 功能优先于完美 → 第163行
  - 先实现基础时钟显示，后续可添加主题切换
- 命名约定 [约定 6] 组件命名 (PascalCase) → 第190行
  - AnalogClock组件使用PascalCase
- 开发需求规范 [规则 1] 生成完整可运行代码 → 第20行
  - 提供完整的SVG时钟实现，包括时针、分针、秒针
- 开发需求规范 [规则 2] 复用现有代码和API → 第48行
  - 复用Card组件和useClock Hook
- 命名约定 [约定 1] 变量命名 (camelCase) → 第20行
  - 变量使用camelCase（angles、center、clockRadius等）

**创建 src/components/Clock/AnalogClock.tsx**:

```typescript
import React, { useMemo } from 'react';
import { Card } from '@/components/Card';
import { useClock } from '@/hooks/use-clock';
import { cn } from '@/utils/cn';

export interface AnalogClockProps {
  size?: number;
  className?: string;
}

/**
 * AnalogClock 模拟时钟组件
 * 使用SVG绘制传统圆形时钟
 */
export const AnalogClock: React.FC<AnalogClockProps> = ({
  size = 200,
  className,
}) => {
  const time = useClock();

  // 计算指针角度
  const angles = useMemo(() => {
    const secondAngle = (time.seconds / 60) * 360;
    const minuteAngle = ((time.minutes + time.seconds / 60) / 60) * 360;
    const hourAngle = ((time.hours % 12 + time.minutes / 60) / 12) * 360;

    return { secondAngle, minuteAngle, hourAngle };
  }, [time.hours, time.minutes, time.seconds]);

  const center = size / 2;
  const clockRadius = size * 0.45;

  return (
    <Card variant="glass" padding="md" className={cn('inline-block', className)}>
      <svg width={size} height={size} viewBox={`0 0 ${size} ${size}`}>
        {/* 时钟背景 */}
        <circle
          cx={center}
          cy={center}
          r={clockRadius}
          fill="rgba(255, 255, 255, 0.6)"
          stroke="#3B82F6"
          strokeWidth="3"
        />

        {/* 刻度线 */}
        {Array.from({ length: 12 }, (_, i) => {
          const angle = (i * 30 - 90) * (Math.PI / 180);
          const x1 = center + Math.cos(angle) * clockRadius * 0.85;
          const y1 = center + Math.sin(angle) * clockRadius * 0.85;
          const x2 = center + Math.cos(angle) * clockRadius * 0.95;
          const y2 = center + Math.sin(angle) * clockRadius * 0.95;

          return (
            <line
              key={i}
              x1={x1}
              y1={y1}
              x2={x2}
              y2={y2}
              stroke="#3B82F6"
              strokeWidth="3"
              strokeLinecap="round"
            />
          );
        })}

        {/* 数字刻度 */}
        {Array.from({ length: 12 }, (_, i) => {
          const hour = i === 0 ? 12 : i;
          const angle = (i * 30 - 90) * (Math.PI / 180);
          const x = center + Math.cos(angle) * clockRadius * 0.7;
          const y = center + Math.sin(angle) * clockRadius * 0.7;

          return (
            <text
              key={i}
              x={x}
              y={y}
              textAnchor="middle"
              dominantBaseline="middle"
              fill="#1E40AF"
              fontSize={size * 0.08}
              fontWeight="bold"
            >
              {hour}
            </text>
          );
        })}

        {/* 时针 */}
        <line
          x1={center}
          y1={center}
          x2={center + Math.cos((angles.hourAngle - 90) * (Math.PI / 180)) * clockRadius * 0.5}
          y2={center + Math.sin((angles.hourAngle - 90) * (Math.PI / 180)) * clockRadius * 0.5}
          stroke="#1E40AF"
          strokeWidth={size * 0.02}
          strokeLinecap="round"
          style={{ transition: 'all 0.5s ease-in-out' }}
        />

        {/* 分针 */}
        <line
          x1={center}
          y1={center}
          x2={center + Math.cos((angles.minuteAngle - 90) * (Math.PI / 180)) * clockRadius * 0.7}
          y2={center + Math.sin((angles.minuteAngle - 90) * (Math.PI / 180)) * clockRadius * 0.7}
          stroke="#3B82F6"
          strokeWidth={size * 0.015}
          strokeLinecap="round"
          style={{ transition: 'all 0.5s ease-in-out' }}
        />

        {/* 秒针 */}
        <line
          x1={center}
          y1={center}
          x2={center + Math.cos((angles.secondAngle - 90) * (Math.PI / 180)) * clockRadius * 0.85}
          y2={center + Math.sin((angles.secondAngle - 90) * (Math.PI / 180)) * clockRadius * 0.85}
          stroke="#EF4444"
          strokeWidth={size * 0.008}
          strokeLinecap="round"
        />

        {/* 中心点 */}
        <circle cx={center} cy={center} r={size * 0.03} fill="#1E40AF" />
      </svg>

      {/* 时间文字显示 */}
      <div className="text-center mt-2 text-blue-900 font-medium">
        {time.formattedTime}
      </div>
    </Card>
  );
};
```

**验收标准**:
- [ ] 指针转动流畅（需求13：时针、分针、秒针平滑移动，使用transition）
- [ ] 角度计算正确（规则1：时针30°/小时，分针6°/分钟，秒针6°/秒）
- [ ] SVG渲染正常（规则1：完整的SVG元素，包括刻度线和数字）
- [ ] 刻度显示清晰（规则11：12个刻度线和数字，遵循标准时钟设计）
- [ ] 遵循标准设计（规则11：传统圆形时钟布局）
- [ ] 组件命名符合规范（约定6：AnalogClock使用PascalCase）
- [ ] 变量命名符合规范（约定1：angles、center使用camelCase）
- [ ] 复用现有组件（规则2：使用Card组件和useClock Hook）
- [ ] 代码可立即运行（规则1：完整实现，无占位符）

---

### 步骤 5.4：创建布谷鸟动画组件

**适用规范**: 
- 开发需求规范 [规则 1] 生成完整可运行代码 → 第20行
  - 提供完整的布谷鸟动画实现，包括波纹效果
- 命名约定 [约定 6] 组件命名 (PascalCase) → 第190行
  - CuckooBird组件使用PascalCase
- 命名约定 [约定 10] 类型/接口命名 (PascalCase) → 第315行
  - CuckooBirdProps接口使用PascalCase + Props后缀
- 开发需求规范 [规则 11] 严格遵循提供的示例 → 第183行
  - 根据需求12实现布谷鸟从时钟弹出的动画效果
- 开发需求规范 [规则 2] 复用现有代码和API → 第48行
  - 复用cn工具函数处理类名

**创建 src/components/Clock/CuckooBird.tsx**:

```typescript
import React from 'react';
import { cn } from '@/utils/cn';

export interface CuckooBirdProps {
  isAnimating: boolean;
  className?: string;
}

/**
 * CuckooBird 布谷鸟动画组件
 * 整点报时时显示动画效果
 */
export const CuckooBird: React.FC<CuckooBirdProps> = ({
  isAnimating,
  className,
}) => {
  return (
    <div 
      className={cn(
        'relative w-24 h-24 mx-auto transition-all duration-500',
        isAnimating && 'scale-110',
        className
      )}
    >
      {/* 布谷鸟图标（使用emoji或SVG） */}
      <div
        className={cn(
          'text-6xl transition-all duration-300',
          isAnimating ? 'animate-bounce' : 'opacity-70'
        )}
      >
        🦆
      </div>

      {/* 声音波纹效果 */}
      {isAnimating && (
        <>
          <div className="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2">
            <div className="w-24 h-24 rounded-full border-4 border-blue-400 animate-ping opacity-75" />
          </div>
          <div className="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2">
            <div className="w-32 h-32 rounded-full border-4 border-blue-300 animate-ping opacity-50" 
                 style={{ animationDelay: '0.2s' }} />
          </div>
        </>
      )}

      {/* 报时文字 */}
      {isAnimating && (
        <div className="absolute -bottom-8 left-1/2 -translate-x-1/2 whitespace-nowrap">
          <div className="text-blue-600 font-bold animate-pulse">
            🔔 整点报时
          </div>
        </div>
      )}
    </div>
  );
};
```

**验收标准**:
- [ ] 动画效果流畅（需求12：布谷鸟弹出动画，使用scale-110和animate-bounce）
- [ ] 波纹扩散自然（规则11：双层波纹效果，使用animate-ping）
- [ ] 整点触发正确（需求12：整点时isAnimating为true，显示动画）
- [ ] 组件命名符合规范（约定6：CuckooBird使用PascalCase）
- [ ] Props接口定义正确（约定10：CuckooBirdProps使用PascalCase + Props后缀）
- [ ] 视觉效果符合设计（需求12：布谷鸟图标、声音波纹、报时文字）
- [ ] 代码可立即运行（规则1：完整实现，包含所有动画效果）
- [ ] 复用工具函数（规则2：使用cn函数）

---

### 步骤 5.5：创建音频管理器

**适用规范**: 
- 命名约定 [约定 3] 类命名 (PascalCase) → 第90行
  - AudioManager类使用PascalCase
- 错误处理规范 [规则 6] 错误恢复策略 → 第216行
  - 音频上下文初始化失败时优雅降级，使用try-catch捕获错误
- 开发需求规范 [规则 13] 只使用真实存在的库 → 第199行
  - 使用浏览器原生Web Audio API，不引入第三方音频库
- 命名约定 [约定 2] 函数命名 (camelCase) → 第54行
  - 方法名使用camelCase，动词开头（playCuckooSound、setVolume等）
- 开发需求规范 [规则 1] 生成完整可运行代码 → 第20行
  - 提供完整的音频管理器实现，包括播放、音量控制、资源清理
- 错误处理规范 [规则 3] 用户友好的错误提示 → 第116行
  - 音频上下文未初始化时提供友好的警告信息
- 命名约定 [约定 1] 变量命名 (camelCase) → 第20行
  - 私有属性使用camelCase（audioContext、gainNode、volume）

**创建 src/utils/audio-manager.ts**:

```typescript
/**
 * AudioManager 音频管理器
 * 管理报时音效播放
 * 遵循命名约定：类名使用 PascalCase
 */
export class AudioManager {
  private audioContext: AudioContext | null = null;
  private gainNode: GainNode | null = null;
  private volume: number = 0.5;

  constructor() {
    this.initAudioContext();
  }

  /**
   * 初始化音频上下文
   */
  private initAudioContext(): void {
    try {
      const AudioContextClass = window.AudioContext || (window as any).webkitAudioContext;
      this.audioContext = new AudioContextClass();
      this.gainNode = this.audioContext.createGain();
      this.gainNode.connect(this.audioContext.destination);
      this.gainNode.gain.value = this.volume;
    } catch (error) {
      console.error('音频上下文初始化失败:', error);
    }
  }

  /**
   * 播放布谷鸟报时音
   * 使用 Web Audio API 生成音效
   */
  async playCuckooSound(times: number = 1): Promise<void> {
    if (!this.audioContext || !this.gainNode) {
      console.warn('音频上下文未初始化');
      return;
    }

    // 恢复音频上下文（某些浏览器需要用户交互后才能播放）
    if (this.audioContext.state === 'suspended') {
      await this.audioContext.resume();
    }

    for (let i = 0; i < times; i++) {
      await this.playBeep();
      if (i < times - 1) {
        await this.sleep(800); // 每次报时间隔800ms
      }
    }
  }

  /**
   * 播放单次蜂鸣音
   */
  private async playBeep(): Promise<void> {
    if (!this.audioContext || !this.gainNode) return;

    const oscillator = this.audioContext.createOscillator();
    const beepGain = this.audioContext.createGain();

    oscillator.connect(beepGain);
    beepGain.connect(this.gainNode);

    // 布谷鸟音效参数（两个音符）
    oscillator.type = 'sine';
    oscillator.frequency.setValueAtTime(800, this.audioContext.currentTime);
    oscillator.frequency.setValueAtTime(600, this.audioContext.currentTime + 0.15);

    // 音量包络
    beepGain.gain.setValueAtTime(0, this.audioContext.currentTime);
    beepGain.gain.linearRampToValueAtTime(0.3, this.audioContext.currentTime + 0.05);
    beepGain.gain.linearRampToValueAtTime(0, this.audioContext.currentTime + 0.3);

    oscillator.start(this.audioContext.currentTime);
    oscillator.stop(this.audioContext.currentTime + 0.3);

    await this.sleep(300);
  }

  /**
   * 设置音量
   */
  setVolume(volume: number): void {
    this.volume = Math.max(0, Math.min(1, volume));
    if (this.gainNode) {
      this.gainNode.gain.value = this.volume;
    }
  }

  /**
   * 获取当前音量
   */
  getVolume(): number {
    return this.volume;
  }

  /**
   * 延迟函数
   */
  private sleep(ms: number): Promise<void> {
    return new Promise(resolve => setTimeout(resolve, ms));
  }

  /**
   * 清理资源
   */
  dispose(): void {
    if (this.audioContext) {
      this.audioContext.close();
      this.audioContext = null;
      this.gainNode = null;
    }
  }
}

// 单例实例
export const audioManager = new AudioManager();
```

**验收标准**:
- [ ] 音频播放正常（需求14：播放布谷鸟音效，支持多次报时）
- [ ] 音量控制有效（需求14：主音量控制，范围0-1）
- [ ] 错误处理完整（规则6：音频上下文初始化失败时优雅降级，使用try-catch）
- [ ] 资源清理正确（规则1：dispose方法关闭音频上下文，释放资源）
- [ ] 使用原生API（规则13：使用Web Audio API，不依赖第三方库）
- [ ] 类命名符合规范（约定3：AudioManager使用PascalCase）
- [ ] 方法命名符合规范（约定2：playCuckooSound、setVolume使用camelCase）
- [ ] 变量命名符合规范（约定1：audioContext、gainNode使用camelCase）
- [ ] 错误提示友好（规则3：console.warn提示音频上下文未初始化）
- [ ] 报时音效准确（需求12：双音符布谷鸟音效，800Hz和600Hz）
- [ ] 报时间隔合理（需求12：每次报时间隔800ms）

---

### 步骤 5.6：创建整合的时钟页面组件

**适用规范**: 
- 开发需求规范 [规则 2] 复用现有代码和API → 第48行
  - 复用DigitalClock、AnalogClock、CuckooBird、Button、Icon组件
  - 复用useHourlyChime Hook和audioManager工具
- 命名约定 [约定 6] 组件命名 (PascalCase) → 第190行
  - ClockPage组件使用PascalCase
- 开发需求规范 [规则 1] 生成完整可运行代码 → 第20行
  - 提供完整的时钟页面实现，整合所有功能
- 命名约定 [约定 2] 函数命名 (camelCase) → 第54行
  - 事件处理函数使用camelCase（handleTestChime等）
- 命名约定 [约定 1] 变量命名 (camelCase) → 第20行
  - 状态变量使用camelCase（chimeEnabled、clockType）
- 开发需求规范 [规则 11] 严格遵循提供的示例 → 第183行
  - 根据需求12、13实现完整的时钟和报时功能

**创建 src/components/Clock/ClockPage.tsx**:

```typescript
import React, { useState } from 'react';
import { DigitalClock } from './DigitalClock';
import { AnalogClock } from './AnalogClock';
import { CuckooBird } from './CuckooBird';
import { Button } from '@/components/Button';
import { Icon } from '@/components/Icon';
import { useHourlyChime } from '@/hooks/use-hourly-chime';
import { audioManager } from '@/utils/audio-manager';

export interface ClockPageProps {
  className?: string;
}

/**
 * ClockPage 时钟页面组件
 * 整合数字时钟、模拟时钟和报时功能
 */
export const ClockPage: React.FC<ClockPageProps> = ({ className }) => {
  const [chimeEnabled, setChimeEnabled] = useState(true);
  const [clockType, setClockType] = useState<'digital' | 'analog'>('digital');

  // 整点报时Hook
  const { isChiming } = useHourlyChime({
    enabled: chimeEnabled,
    startHour: 8,
    endHour: 22,
    onChime: (hour) => {
      console.log(`整点报时: ${hour}点`);
      // 播放报时音（报时次数为当前小时数，12小时制）
      const chimeTimes = hour > 12 ? hour - 12 : hour;
      audioManager.playCuckooSound(chimeTimes);
    },
  });

  /**
   * 测试报时功能
   */
  const handleTestChime = () => {
    const now = new Date();
    const hour = now.getHours();
    const chimeTimes = hour > 12 ? hour - 12 : hour;
    audioManager.playCuckooSound(chimeTimes);
  };

  return (
    <div className={className}>
      {/* 控制按钮 */}
      <div className="flex justify-between items-center mb-4">
        <div className="space-x-2">
          <Button
            variant={clockType === 'digital' ? 'primary' : 'ghost'}
            size="sm"
            onClick={() => setClockType('digital')}
          >
            数字时钟
          </Button>
          <Button
            variant={clockType === 'analog' ? 'primary' : 'ghost'}
            size="sm"
            onClick={() => setClockType('analog')}
          >
            模拟时钟
          </Button>
        </div>

        <div className="space-x-2">
          <Button
            variant={chimeEnabled ? 'primary' : 'secondary'}
            size="sm"
            leftIcon={<Icon name={chimeEnabled ? 'Bell' : 'BellOff'} size={16} />}
            onClick={() => setChimeEnabled(!chimeEnabled)}
          >
            {chimeEnabled ? '报时开' : '报时关'}
          </Button>
          <Button
            variant="secondary"
            size="sm"
            leftIcon={<Icon name="Volume2" size={16} />}
            onClick={handleTestChime}
          >
            测试报时
          </Button>
        </div>
      </div>

      {/* 布谷鸟动画 */}
      <CuckooBird isAnimating={isChiming} className="mb-6" />

      {/* 时钟显示 */}
      <div className="flex justify-center">
        {clockType === 'digital' ? (
          <DigitalClock showSeconds showDate className="w-full max-w-md" />
        ) : (
          <AnalogClock size={300} />
        )}
      </div>

      {/* 报时说明 */}
      <div className="mt-6 text-center text-sm text-blue-600">
        <Icon name="Info" size={16} className="inline mr-1" />
        报时时间：每天 8:00 - 22:00
      </div>
    </div>
  );
};
```

**验收标准**:
- [ ] 时钟切换正常（需求13：数字时钟和模拟时钟两种样式切换）
- [ ] 报时开关有效（需求12：报时开关控制整点报时功能）
- [ ] 测试报时功能正常（规则1：手动触发报时，播放对应小时数的音效）
- [ ] 整点自动报时正确（需求12：整点00分00秒自动触发，播放对应次数）
- [ ] 组件命名符合规范（约定6：ClockPage使用PascalCase）
- [ ] 函数命名符合规范（约定2：handleTestChime使用camelCase）
- [ ] 变量命名符合规范（约定1：chimeEnabled、clockType使用camelCase）
- [ ] 复用现有组件（规则2：使用DigitalClock、AnalogClock、CuckooBird、Button、Icon）
- [ ] 复用Hooks和工具（规则2：使用useHourlyChime和audioManager）
- [ ] 报时时间段正确（需求12：8:00-22:00时间段控制）
- [ ] 报时次数正确（需求12：12小时制，报时次数对应当前小时数）
- [ ] 代码可立即运行（规则1：完整实现，包含所有必要的import和状态管理）

---

### 步骤 5.7：创建组件导出

**适用规范**: 
- 开发需求规范 [规则 2] 复用现有代码和API → 第48行
  - 创建统一导出文件便于其他模块复用时钟组件
- 命名约定 [约定 5] 文件命名 (kebab-case) → 第154行
  - 使用index.ts作为模块入口文件
- 开发需求规范 [规则 1] 生成完整可运行代码 → 第20行
  - 导出所有时钟组件、Hooks和类型定义

**创建 src/components/Clock/index.ts**:

```typescript
export { DigitalClock } from './DigitalClock';
export type { DigitalClockProps } from './DigitalClock';

export { AnalogClock } from './AnalogClock';
export type { AnalogClockProps } from './AnalogClock';

export { CuckooBird } from './CuckooBird';
export type { CuckooBirdProps } from './CuckooBird';

export { ClockPage } from './ClockPage';
export type { ClockPageProps } from './ClockPage';
```

**更新 src/hooks/index.ts**:

```typescript
export { useClock } from './use-clock';
export type { ClockTime } from './use-clock';

export { useHourlyChime } from './use-hourly-chime';
export type { HourlyChimeOptions } from './use-hourly-chime';
```

**验收标准**:
- [ ] 所有组件正确导出（规则1：导出DigitalClock、AnalogClock、CuckooBird、ClockPage）
- [ ] 所有Hooks正确导出（规则1：导出useClock、useHourlyChime）
- [ ] 类型定义正确导出（规则1：导出所有Props接口和Options接口）
- [ ] 导入路径简洁（规则2：使用@/components/Clock和@/hooks统一导入）
- [ ] 文件命名符合规范（约定5：使用index.ts）

---

## 📊 阶段验收清单

### 功能验收
- [ ] 数字时钟显示正确
- [ ] 模拟时钟显示正确
- [ ] 整点报时触发准确
- [ ] 报时音效播放正常
- [ ] 布谷鸟动画流畅

### Hook验收
- [ ] useClock 数据更新正确
- [ ] useHourlyChime 触发准确
- [ ] Hook命名符合规范

### 音频验证
- [ ] Web Audio API 正常工作
- [ ] 音量控制有效
- [ ] 多次报时间隔正确

### 代码质量验收
- [ ] 组件命名符合规范
- [ ] 函数命名符合规范
- [ ] 类型定义完整
- [ ] 错误处理完善

---

## 📝 下一步

完成本阶段后，请继续阅读：
- **阶段6：测试和质量保证**
- **阶段7：文档和部署**

---

---

## 📚 规范文件快速参考

本文档引用的所有规范条款详见:

| 规范类别 | 文件路径 | 规则编号 | 起始行 |
|---------|---------|---------|--------|
| 开发需求规范 | `.qoder/rules/requirements-spec.zh-CN.md` | 规则 1 | 第20行 |
| 开发需求规范 | `.qoder/rules/requirements-spec.zh-CN.md` | 规则 11 | 第183行 |
| 开发需求规范 | `.qoder/rules/requirements-spec.zh-CN.md` | 规则 12 | 第191行 |
| 命名约定 | `.qoder/rules/naming-conventions.zh-CN.md` | 约定 1 | 第20行 |
| 命名约定 | `.qoder/rules/naming-conventions.zh-CN.md` | 约定 2 | 第54行 |
| 命名约定 | `.qoder/rules/naming-conventions.zh-CN.md` | 约定 3 | 第90行 |
| 命名约定 | `.qoder/rules/naming-conventions.zh-CN.md` | 约定 6 | 第190行 |
| 错误处理规范 | `.qoder/rules/error-handling-spec.zh-CN.md` | 规则 3 | 第116行 |
| 错误处理规范 | `.qoder/rules/error-handling-spec.zh-CN.md` | 规则 6 | 第216行 |
| 需求文档 | `requirements.md` | 需求12 | 第158-171行 |
| 需求文档 | `requirements.md` | 需求13 | 第172-186行 |
| 需求文档 | `requirements.md` | 需求14 | 第187-200行 |

**使用方法**:
1. 在IDE中使用 `Ctrl+G` (或 `Cmd+G`) 跳转到指定行号
2. 或在Qoder中使用 `@文件路径` 直接打开规范文件

---

**文档版本**: v1.1  
**更新日期**: 2025-11-13  
**维护团队**: 天气鸭开发团队

### 修订历史
- v1.1 (2025-11-13)
  - 补充标准章节（概述/接口/参数/响应/错误/示例）
  - 新增可直接运行的时钟与报时示例代码
  - 明确音频上下文恢复与降级策略
  - 更新版本号与日期
