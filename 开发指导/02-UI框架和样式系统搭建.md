# 天气鸭项目开发指导 - 阶段2：UI框架和样式系统搭建

## 📋 阶段概述

本阶段将搭建基于 Tailwind CSS 的样式系统，实现设计系统的核心组件，并建立UI组件库的基础架构。

---

## 🎯 核心目标

- ✅ 配置 Tailwind CSS 样式框架
- ✅ 实现毛玻璃效果和现代化卡片布局
- ✅ 创建可复用的基础UI组件
- ✅ 建立设计令牌（Design Tokens）系统

---

## 📖 适用规范清单

### 核心规范
- **开发需求规范**
  - [规则 1] 生成完整可运行代码
  - [规则 2] 复用现有代码和API
  - [规则 12] 尊重项目命名约定

- **命名约定**
  - [约定 2] 函数/方法命名 (camelCase, 动词开头)
  - [约定 3] 类命名 (PascalCase, 名词)
  - [约定 5] 文件命名 (kebab-case)
  - [约定 6] 组件命名 (PascalCase)

### 质量规范
- **测试规范**
  - [规则 1] 测试完整性
  - [规则 5] 测试命名约定

---

## 🚀 实施步骤

### 步骤 2.1：安装和配置 Tailwind CSS

**适用规范**: 开发需求规范 [规则 13] 只使用真实存在的库

**安装依赖**:

```powershell
# 安装 Tailwind CSS（基于 Context7 验证：信任分数10分）
npm install -D tailwindcss@3 postcss autoprefixer

# 初始化 Tailwind 配置
npx tailwindcss init -p
```

**配置 tailwind.config.js**:

```javascript
/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {
      colors: {
        // 品牌色系（基于原型设计）
        primary: {
          50: '#f0f9ff',
          100: '#e0f2fe',
          200: '#bae6fd',
          300: '#7dd3fc',
          400: '#38bdf8',
          500: '#0ea5e9', // 主色调
          600: '#0284c7',
          700: '#0369a1',
          800: '#075985',
          900: '#0c4a6e',
        },
        // 天气状态色
        weather: {
          sunny: '#FFB300',
          cloudy: '#90CAF9',
          rainy: '#42A5F5',
          snowy: '#E3F2FD',
          foggy: '#B0BEC5',
        },
        // 功能色
        success: '#10b981',
        warning: '#f59e0b',
        error: '#ef4444',
        info: '#3b82f6',
      },
      backdropBlur: {
        xs: '2px',
      },
      borderRadius: {
        '4xl': '2rem',
      },
      boxShadow: {
        'glass': '0 8px 32px 0 rgba(6, 132, 255, 0.1)',
        'card': '0 4px 12px rgba(6, 132, 255, 0.08)',
        'card-hover': '0 8px 22px rgba(6, 132, 255, 0.18)',
      },
      animation: {
        'fade-in': 'fadeIn 0.6s cubic-bezier(0.22, 0.61, 0.36, 1)',
        'slide-up': 'slideUp 0.4s cubic-bezier(0.22, 0.61, 0.36, 1)',
        'spin-slow': 'spin 20s linear infinite',
        'pulse-subtle': 'pulseSubtle 3s ease-in-out infinite',
      },
      keyframes: {
        fadeIn: {
          '0%': { opacity: '0', transform: 'translateY(10px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        slideUp: {
          '0%': { transform: 'translateY(20px)', opacity: '0' },
          '100%': { transform: 'translateY(0)', opacity: '1' },
        },
        pulseSubtle: {
          '0%, 100%': { opacity: '1' },
          '50%': { opacity: '0.8' },
        },
      },
    },
  },
  plugins: [],
}
```

**更新 src/index.css**:

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

/* 自定义基础样式 */
@layer base {
  body {
    @apply font-sans antialiased;
    font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
  }

  * {
    @apply transition-timing-[cubic-bezier(0.22,0.61,0.36,1)];
  }
}

/* 毛玻璃效果工具类 */
@layer utilities {
  .glass {
    @apply bg-white/40 backdrop-blur-md;
  }

  .glass-card {
    @apply glass rounded-2xl shadow-glass;
  }

  .text-gradient {
    @apply bg-gradient-to-r from-blue-600 to-cyan-600 bg-clip-text text-transparent;
  }
}

/* 自定义组件样式 */
@layer components {
  .weather-card {
    @apply glass-card p-4 transition-all duration-400;
    @apply hover:-translate-y-1 hover:shadow-card-hover;
  }

  .app-container {
    @apply w-[375px] h-[812px] flex flex-col;
    @apply overflow-hidden rounded-3xl shadow-2xl;
    background: linear-gradient(180deg, #f0f8ff 0%, #d3e8ff 100%);
  }
}
```

**验收标准**:
- [ ] Tailwind CSS 配置正确
- [ ] 自定义颜色和主题生效
- [ ] 毛玻璃效果正常显示
- [ ] 动画效果流畅

---

### 步骤 2.2：创建设计令牌系统

**适用规范**: 命名约定 [约定 4] 常量命名 (UPPER_SNAKE_CASE)

**创建 src/constants/design-tokens.ts**:

```typescript
/**
 * 设计令牌 - 集中管理设计系统的基础值
 * 遵循命名约定：UPPER_SNAKE_CASE
 */

// 间距系统
export const SPACING = {
  XS: '4px',
  SM: '8px',
  MD: '16px',
  LG: '24px',
  XL: '32px',
  XXL: '48px',
} as const;

// 圆角系统
export const BORDER_RADIUS = {
  SM: '8px',
  MD: '12px',
  LG: '16px',
  XL: '20px',
  XXL: '24px',
  FULL: '9999px',
} as const;

// 阴影系统
export const SHADOWS = {
  GLASS: '0 8px 32px 0 rgba(6, 132, 255, 0.1)',
  CARD: '0 4px 12px rgba(6, 132, 255, 0.08)',
  CARD_HOVER: '0 8px 22px rgba(6, 132, 255, 0.18)',
  ELEVATED: '0 20px 40px rgba(0, 0, 0, 0.2)',
} as const;

// 动画时长
export const ANIMATION_DURATION = {
  FAST: 200,
  NORMAL: 300,
  SLOW: 500,
  VERY_SLOW: 1000,
} as const;

// 缓动函数
export const EASING = {
  EASE_OUT: 'cubic-bezier(0.22, 0.61, 0.36, 1)',
  EASE_IN_OUT: 'cubic-bezier(0.4, 0, 0.2, 1)',
  SPRING: 'cubic-bezier(0.34, 1.56, 0.64, 1)',
} as const;

// Z-index 层级
export const Z_INDEX = {
  BASE: 0,
  DROPDOWN: 1000,
  STICKY: 1020,
  FIXED: 1030,
  MODAL_BACKDROP: 1040,
  MODAL: 1050,
  POPOVER: 1060,
  TOOLTIP: 1070,
} as const;

// 断点系统
export const BREAKPOINTS = {
  SM: 640,
  MD: 768,
  LG: 1024,
  XL: 1280,
  XXL: 1536,
} as const;
```

**验收标准**:
- [ ] 所有常量使用 UPPER_SNAKE_CASE 命名
- [ ] 使用 as const 确保类型安全
- [ ] 设计令牌完整且有组织

---

### 步骤 2.3：创建基础UI组件 - Button

**适用规范**: 
- 命名约定 [约定 6] 组件命名 (PascalCase)
- 命名约定 [约定 2] 函数命名 (camelCase)

**创建 src/components/Button/Button.tsx**:

```typescript
import React from 'react';
import { cn } from '@/utils/cn';

export interface ButtonProps extends React.ButtonHTMLAttributes<HTMLButtonElement> {
  variant?: 'primary' | 'secondary' | 'ghost';
  size?: 'sm' | 'md' | 'lg';
  isLoading?: boolean;
  leftIcon?: React.ReactNode;
  rightIcon?: React.ReactNode;
}

/**
 * Button 组件
 * 遵循命名约定：组件名使用 PascalCase
 */
export const Button: React.FC<ButtonProps> = ({
  variant = 'primary',
  size = 'md',
  isLoading = false,
  leftIcon,
  rightIcon,
  children,
  className,
  disabled,
  ...props
}) => {
  const baseStyles = 'inline-flex items-center justify-center rounded-lg font-medium transition-all';
  
  const variantStyles = {
    primary: 'bg-blue-500 text-white hover:bg-blue-600 active:bg-blue-700',
    secondary: 'bg-white/20 backdrop-blur text-blue-900 hover:bg-white/30',
    ghost: 'bg-transparent text-blue-600 hover:bg-blue-50',
  };
  
  const sizeStyles = {
    sm: 'px-3 py-1.5 text-sm',
    md: 'px-4 py-2 text-base',
    lg: 'px-6 py-3 text-lg',
  };

  return (
    <button
      className={cn(
        baseStyles,
        variantStyles[variant],
        sizeStyles[size],
        disabled && 'opacity-50 cursor-not-allowed',
        className
      )}
      disabled={disabled || isLoading}
      {...props}
    >
      {isLoading ? (
        <span className="inline-block w-4 h-4 border-2 border-current border-t-transparent rounded-full animate-spin mr-2" />
      ) : leftIcon ? (
        <span className="mr-2">{leftIcon}</span>
      ) : null}
      {children}
      {rightIcon && <span className="ml-2">{rightIcon}</span>}
    </button>
  );
};
```

**创建 src/components/Button/index.ts**:

```typescript
export { Button } from './Button';
export type { ButtonProps } from './Button';
```

**创建工具函数 src/utils/cn.ts**:

```typescript
import { type ClassValue, clsx } from 'clsx';
import { twMerge } from 'tailwind-merge';

/**
 * 合并 Tailwind CSS 类名的工具函数
 * 遵循命名约定：函数名使用 camelCase
 */
export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

**安装依赖**:

```powershell
npm install clsx tailwind-merge
```

**验收标准**:
- [ ] 组件名使用 PascalCase
- [ ] 函数名使用 camelCase
- [ ] 组件功能完整
- [ ] 类型定义正确

---

### 步骤 2.4：创建基础UI组件 - Card

**适用规范**: 命名约定 [约定 6] 组件命名

**创建 src/components/Card/Card.tsx**:

```typescript
import React from 'react';
import { cn } from '@/utils/cn';

export interface CardProps extends React.HTMLAttributes<HTMLDivElement> {
  variant?: 'default' | 'glass' | 'elevated';
  hoverable?: boolean;
  padding?: 'none' | 'sm' | 'md' | 'lg';
}

/**
 * Card 卡片组件
 * 支持毛玻璃效果和悬停动画
 */
export const Card: React.FC<CardProps> = ({
  variant = 'glass',
  hoverable = true,
  padding = 'md',
  className,
  children,
  ...props
}) => {
  const baseStyles = 'rounded-2xl transition-all duration-300';
  
  const variantStyles = {
    default: 'bg-white shadow-card',
    glass: 'bg-white/40 backdrop-blur-md shadow-glass',
    elevated: 'bg-white shadow-elevated',
  };
  
  const paddingStyles = {
    none: '',
    sm: 'p-3',
    md: 'p-4',
    lg: 'p-6',
  };

  const hoverStyles = hoverable 
    ? 'hover:-translate-y-1 hover:shadow-card-hover cursor-pointer'
    : '';

  return (
    <div
      className={cn(
        baseStyles,
        variantStyles[variant],
        paddingStyles[padding],
        hoverStyles,
        className
      )}
      {...props}
    >
      {children}
    </div>
  );
};
```

**创建 src/components/Card/index.ts**:

```typescript
export { Card } from './Card';
export type { CardProps } from './Card';
```

**验收标准**:
- [ ] 毛玻璃效果正常
- [ ] 悬停动画流畅
- [ ] 组件可复用

---

### 步骤 2.5：创建图标组件系统

**适用规范**: 开发需求规范 [规则 13] 只使用真实存在的库

**安装 Lucide React**:

```powershell
# Lucide React - 基于 Context7 验证的真实库
npm install lucide-react
```

**创建 src/components/Icon/Icon.tsx**:

```typescript
import React from 'react';
import * as LucideIcons from 'lucide-react';
import { cn } from '@/utils/cn';

export type IconName = keyof typeof LucideIcons;

export interface IconProps {
  name: IconName;
  size?: number | string;
  color?: string;
  className?: string;
  strokeWidth?: number;
}

/**
 * Icon 图标组件
 * 基于 Lucide React 图标库
 */
export const Icon: React.FC<IconProps> = ({
  name,
  size = 24,
  color,
  className,
  strokeWidth = 2,
}) => {
  const IconComponent = LucideIcons[name] as React.ComponentType<any>;
  
  if (!IconComponent) {
    console.warn(`Icon "${name}" not found`);
    return null;
  }

  return (
    <IconComponent
      size={size}
      color={color}
      strokeWidth={strokeWidth}
      className={cn('inline-block', className)}
    />
  );
};
```

**创建 src/components/Icon/index.ts**:

```typescript
export { Icon } from './Icon';
export type { IconProps, IconName } from './Icon';
```

**验收标准**:
- [ ] 图标库正确安装
- [ ] 图标可以正常显示
- [ ] 支持自定义大小和颜色

---

### 步骤 2.6：创建组件库索引

**适用规范**: 开发需求规范 [规则 2] 复用现有代码和API

**创建 src/components/index.ts**:

```typescript
/**
 * 组件库统一导出
 * 便于其他模块导入使用
 */

export { Button } from './Button';
export type { ButtonProps } from './Button';

export { Card } from './Card';
export type { CardProps } from './Card';

export { Icon } from './Icon';
export type { IconProps, IconName } from './Icon';

// 后续添加更多组件...
```

**验收标准**:
- [ ] 所有组件统一导出
- [ ] 类型定义正确导出
- [ ] 导入路径简洁

---

### 步骤 2.7：创建布局组件

**适用规范**: 命名约定 [约定 3] 类命名

**创建 src/components/Layout/AppContainer.tsx**:

```typescript
import React from 'react';
import { cn } from '@/utils/cn';

export interface AppContainerProps {
  children: React.ReactNode;
  className?: string;
}

/**
 * AppContainer 应用容器组件
 * 提供统一的应用外层布局
 */
export const AppContainer: React.FC<AppContainerProps> = ({
  children,
  className,
}) => {
  return (
    <div className={cn('app-container', className)}>
      {children}
    </div>
  );
};
```

**创建 src/components/Layout/Header.tsx**:

```typescript
import React from 'react';
import { cn } from '@/utils/cn';
import { Icon } from '@/components/Icon';

export interface HeaderProps {
  title?: string;
  subtitle?: string;
  onRefresh?: () => void;
  onSettings?: () => void;
  className?: string;
}

/**
 * Header 标题栏组件
 * 包含应用标题和操作按钮
 */
export const Header: React.FC<HeaderProps> = ({
  title = '天气鸭',
  subtitle = '智能天气助手',
  onRefresh,
  onSettings,
  className,
}) => {
  return (
    <header 
      className={cn(
        'bg-gradient-to-r from-blue-500 to-blue-400',
        'py-4 px-6 flex justify-between items-center',
        'h-14 flex-shrink-0',
        className
      )}
    >
      <div className="flex items-center space-x-3">
        <div className="text-2xl">🦆</div>
        <div>
          <h1 className="text-white text-xl font-bold tracking-wider">
            {title}
          </h1>
          <p className="text-blue-100 text-xs">{subtitle}</p>
        </div>
      </div>
      
      <div className="flex space-x-2">
        {onRefresh && (
          <button
            onClick={onRefresh}
            className="flex items-center justify-center w-9 h-9 rounded-full bg-white/20 hover:bg-white/30 transition"
            aria-label="刷新天气"
          >
            <Icon name="RefreshCw" size={18} color="white" />
          </button>
        )}
        
        {onSettings && (
          <button
            onClick={onSettings}
            className="flex items-center justify-center w-9 h-9 rounded-full bg-white/20 hover:bg-white/30 transition"
            aria-label="打开设置"
          >
            <Icon name="Settings" size={18} color="white" />
          </button>
        )}
      </div>
    </header>
  );
};
```

**创建 src/components/Layout/index.ts**:

```typescript
export { AppContainer } from './AppContainer';
export type { AppContainerProps } from './AppContainer';

export { Header } from './Header';
export type { HeaderProps } from './Header';
```

**验收标准**:
- [ ] 布局组件功能完整
- [ ] 响应式设计正确
- [ ] 按钮交互正常

---

### 步骤 2.8：更新主应用组件

**适用规范**: 开发需求规范 [规则 1] 生成完整可运行代码

**更新 src/App.tsx**:

```typescript
import React, { useState } from 'react';
import { AppContainer, Header } from '@/components/Layout';
import { Card } from '@/components/Card';
import { Button } from '@/components/Button';
import { Icon } from '@/components/Icon';

function App() {
  const [isRefreshing, setIsRefreshing] = useState(false);

  const handleRefresh = () => {
    setIsRefreshing(true);
    console.log('刷新天气数据...');
    
    // 模拟刷新
    setTimeout(() => {
      setIsRefreshing(false);
    }, 1000);
  };

  const handleSettings = () => {
    console.log('打开设置面板...');
  };

  return (
    <AppContainer>
      <Header 
        onRefresh={handleRefresh}
        onSettings={handleSettings}
      />
      
      <main className="flex-1 overflow-y-auto p-5">
        <Card variant="glass" className="mb-5">
          <h2 className="text-xl font-bold text-blue-900 mb-2">
            UI 框架搭建完成
          </h2>
          <p className="text-blue-600 mb-4">
            Tailwind CSS 和基础组件库已就绪
          </p>
          
          <div className="space-y-3">
            <Button 
              variant="primary" 
              leftIcon={<Icon name="Cloud" size={18} />}
              isLoading={isRefreshing}
            >
              刷新天气
            </Button>
            
            <Button variant="secondary">
              次要按钮
            </Button>
            
            <Button variant="ghost">
              幽灵按钮
            </Button>
          </div>
        </Card>
        
        <div className="grid grid-cols-2 gap-3">
          <Card hoverable>
            <div className="text-center">
              <Icon name="Droplets" size={32} color="#3b82f6" />
              <div className="mt-2 text-sm text-blue-600">湿度</div>
              <div className="text-xl font-bold text-blue-900">62%</div>
            </div>
          </Card>
          
          <Card hoverable>
            <div className="text-center">
              <Icon name="Wind" size={32} color="#3b82f6" />
              <div className="mt-2 text-sm text-blue-600">风速</div>
              <div className="text-xl font-bold text-blue-900">18km/h</div>
            </div>
          </Card>
        </div>
      </main>
    </AppContainer>
  );
}

export default App;
```

**验收标准**:
- [ ] 应用正常显示
- [ ] 所有组件正常工作
- [ ] 动画效果流畅
- [ ] 交互响应正常

---

### 步骤 2.9：编写组件测试

**适用规范**: 
- 测试规范 [规则 1] 测试完整性
- 测试规范 [规则 5] 测试命名约定

**安装测试依赖**:

```powershell
npm install -D vitest @testing-library/react @testing-library/jest-dom @testing-library/user-event jsdom
```

**创建测试配置 vitest.config.ts**:

```typescript
import { defineConfig } from 'vitest/config';
import react from '@vitejs/plugin-react';
import path from 'path';

export default defineConfig({
  plugins: [react()],
  test: {
    environment: 'jsdom',
    setupFiles: ['./src/test/setup.ts'],
    globals: true,
  },
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
});
```

**创建测试设置 src/test/setup.ts**:

```typescript
import '@testing-library/jest-dom';
```

**创建 Button 组件测试 src/components/Button/Button.test.tsx**:

```typescript
import { describe, it, expect, vi } from 'vitest';
import { render, screen } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { Button } from './Button';

describe('Button', () => {
  it('should render button with text', () => {
    render(<Button>Click me</Button>);
    expect(screen.getByText('Click me')).toBeInTheDocument();
  });

  it('should call onClick when clicked', async () => {
    const handleClick = vi.fn();
    const user = userEvent.setup();
    
    render(<Button onClick={handleClick}>Click me</Button>);
    
    await user.click(screen.getByText('Click me'));
    expect(handleClick).toHaveBeenCalledOnce();
  });

  it('should be disabled when disabled prop is true', () => {
    render(<Button disabled>Click me</Button>);
    expect(screen.getByRole('button')).toBeDisabled();
  });

  it('should show loading state', () => {
    render(<Button isLoading>Click me</Button>);
    expect(screen.getByRole('button')).toBeDisabled();
  });
});
```

**运行测试**:

```powershell
npm run test
```

**验收标准**:
- [ ] 测试文件使用 .test.tsx 后缀
- [ ] 测试用例描述清晰
- [ ] 所有测试通过
- [ ] 测试覆盖核心功能

---

## 📊 阶段验收清单

### 样式系统验收
- [ ] Tailwind CSS 配置正确
- [ ] 自定义主题生效
- [ ] 毛玻璃效果正常
- [ ] 动画效果流畅
- [ ] 响应式设计正常

### 组件库验收
- [ ] Button 组件功能完整
- [ ] Card 组件功能完整
- [ ] Icon 组件功能完整
- [ ] Layout 组件功能完整
- [ ] 所有组件命名符合规范

### 代码质量验收
- [ ] 组件使用 PascalCase 命名
- [ ] 函数使用 camelCase 命名
- [ ] 常量使用 UPPER_SNAKE_CASE 命名
- [ ] 文件使用 kebab-case 命名
- [ ] TypeScript 类型定义完整

### 测试验收
- [ ] 测试文件命名正确
- [ ] 测试用例完整
- [ ] 所有测试通过
- [ ] 测试覆盖率达标

---

## 📝 下一步

完成本阶段后，请继续阅读：
- **阶段3：天气数据集成和API对接**
- **阶段4：时钟和报时功能实现**

---

**文档版本**: v1.0  
**更新日期**: 2024-01-01  
**维护团队**: 天气鸭开发团队
